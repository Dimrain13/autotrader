# MomentumX (Internal-Trader) — PRD & Remediation Log

## Original Problem Statement
Internal-Trader (MomentumX): React + FastAPI + MongoDB algorithmic trading app,
self-hosted on Windows Server VPS (RDP), broker Alpaca. Goal: remediate a
5-phase security/correctness/reliability audit, in order, staying on PAPER
trading until Phases 1-3 are done and verified. Source repo:
https://github.com/Dimrain13/Internal-trader

Phases: 1) Critical Security, 2) Critical Trading Correctness,
3) Architecture/Reliability, 4) Logic Quality/Trust, 5) Windows-VPS Deployment.

## User Choices (gathered via ask_human)
- Remote access: RDP/localhost only, no external exposure needed.
- Auto-trader: keep full auto-trader with real background loop.
- Daily-loss kill switch: YES, hard kill switch (blocks new BUY orders).
- Session scope: Phase 1-3 only (Security + Trading Correctness + Architecture/Reliability). Phase 4/5 deferred.
- Alpaca paper credentials: NOT provided (only a partial key ID, no secret) — ALPACA_API_KEY/SECRET intentionally blank; app gracefully errors "Alpaca API not configured" instead of faking data.

## Architecture
- Backend: FastAPI (`/app/backend/server.py`), services in `/app/backend/services/*`
- New shared modules: `database.py` (Motor client), `auth.py` (bearer token dependency)
- Frontend: React (`/app/frontend/src`), axios global interceptor in `lib/axiosConfig.js`
- DB: MongoDB — `auto_trader_state`, `monitored_positions`, `trade_history`, `missed_opportunities`, `app_config`, `scans` collections

## What's Been Implemented (2026-07-10)

### Phase 1 — Critical Security ✅
1. Bearer token auth (`API_ACCESS_TOKEN` in `.env`) via `Depends(verify_token)` on entire `api_router`. Frontend token gate screen + axios interceptor + 401 auto-logout.
2. `/api/settings` GET masks both `api_key` and `secret_key` (never plaintext). POST only accepts `sma_short`/`sma_long` (Pydantic `SmaSettingsUpdate`), persisted to Mongo `app_config` — runtime `.env` rewriting of secrets removed entirely.
3. CORS: explicit `CORS_ORIGINS` env list, no `*` default, verified reflects only whitelisted origins.
4. `slowapi` rate limiting (20/min orders, 10/min scan/cancel/process, 20/min demo). `TradeOrder`/`ScanCriteria` now strict Pydantic models (qty>0, side literal buy/sell, pct bounds, price bounds).

### Phase 2 — Trading Correctness ✅
5. `alpaca_service.py`: `paper` flag driven by `ALPACA_PAPER` env or inferred from `base_url`; bold LIVE_TRADING warning banner + startup log.
6. Removed all synthetic/random-OHLC generation (`get_bars`, `check_entry_conditions`). Standardized on `get_bars_with_fallback` (Alpaca→Yahoo→Nasdaq, real data only); explicit "no data" errors, auto-trader skips symbol on missing data.
7. `close_close` NameError bug: moot — entire fake-data block removed.
8. Removed `DAY_TRADING_MODE` fake buying-power simulation from `/account`.

### Phase 3 — Architecture/Reliability ✅
9. Real `asyncio.create_task(auto_trader_loop())` on startup, 60s interval, gated by `auto_trader.active`. `/auto-trader/process` kept as manual trigger only. Fixed pre-existing arity bug (was called with 3 args, signature takes 2).
10. `auto_trader` + `position_monitor` state (`open_positions`, `daily_pnl`, `consecutive_losses`, `exited_today`, `monitored_positions`) persisted to MongoDB, restored on startup — verified surviving an actual backend restart.
11. Wrapped all sync Alpaca/requests calls in `asyncio.to_thread` across `server.py` and all services. Removed blocking `asyncio.sleep(2)` fill-wait in `place_order`.
12. `trade_history` and `missed_opportunities` migrated from flat JSON files to MongoDB collections (fully async).

### Extras implemented
- Hard server-side kill switch: blocks new BUY orders via `/api/orders` once daily loss limit / consecutive losses hit (per user's explicit choice), in addition to auto-trader's own gating.
- Real fill price (`filled_avg_price`) used for P&L logging instead of last quote, where available.
- Fixed missing `alpaca_service.cancel_all_orders()` method (was called by EOD closer but never defined).
- Phase 5 quick wins: removed committed `.gitconfig` and junk `=` file; confirmed `.env` gitignored.

### Testing
- Existing `test_auto_trader_exit_logic.py` (16 tests): PASS.
- New `test_security_and_trading.py` (25 tests, added by testing agent): PASS — covers auth, CORS, rate limiting, validation, no-fake-data, kill switch, state persistence, unified persistence.
- Fixed 1 HIGH bug found by testing: logout button was unreachable (nested inside `{account && ...}`), hoisted PAPER badge + logout button outside that conditional.

## Session 4 Update (2026-07-10) — Token Confusion UX Fix
- User reported: "what API access token is needed to unlock the site? PKRBZGHKVX2SGHWQZZVRJLXRPN didn't work" — this was the Alpaca broker key, not the site's `API_ACCESS_TOKEN`. Correctly rejected by design (broker credentials must never double as site auth).
- Fix: clarified `TokenGate` copy in `frontend/src/App.js` (intro text, footer note, error message) to explicitly distinguish the site's `API_ACCESS_TOKEN` (backend/.env) from the Alpaca API key/secret.
- Testing agent verified 100% pass (41/41 pytest + full TokenGate UX flow) — no functional regression, copy renders correctly.
- Cleaned up test-artifact positions/trade-history entries left in the live paper account from verification testing.

## Session 5 Update (2026-07-10) — VPS Deployment Readiness Check
- Ran `deployment_agent` health check. Result: no real blockers for the Windows VPS target. One flagged "BLOCKER" (CORS_ORIGINS should be `*`) is a **false positive** — that rule is calibrated for Emergent's own `*.emergent.host` hosting, not this app's actual self-hosted-VPS target, and applying it would violate the explicit Phase 1 #3 security requirement (no `*` + credentials). Correctly NOT applied.
- Fixed legitimate finding: added `/memory/` and `test_credentials.md` to `.gitignore` (contains live Alpaca paper secret + API_ACCESS_TOKEN, was not previously excluded from git).
- Confirmed installed stack: Python 3.11.15, Node 20.20.2/Yarn 1.22.22, MongoDB 7.0.37; key backend deps `fastapi==0.110.1`, `alpaca-py==0.30.1`, `motor==3.3.1`, `slowapi==0.1.10`, `pydantic==2.12.4`; key frontend deps `react==19`, `react-router-dom==7`, `axios`, `lightweight-charts==4.1.3`, `tailwindcss==3.4`.
- Backlog (non-blocking): unbounded Mongo `.to_list(length=10000)` reads in `trade_history_service.get_analytics()` / `missed_opportunities_service.get_analytics()` — add projections/date-bounding as trade volume grows.

## Session 6 Update (2026-07-10) — Performance + Linux Deployment
- User moving VPS target to Linux (SSH port-forward for access, same 127.0.0.1-only security model as RDP). Added `/app/deploy/linux/` (systemd services, `start.sh`, install/uninstall scripts, README) alongside the existing Windows artifacts. Root README now links both.
- Performance: `google_news_service.py` now uses a pooled `requests.Session()` + 3-minute TTL cache per (symbol, company_name, limit) - repeated news lookups ~30x faster (645ms → ~20-100ms cache hit). Fixed a latent bug where two early-return paths returned a bare tuple instead of the documented `{'has_news', 'articles'}` dict (would have crashed callers if ever hit).
- `alpaca_service.py`: `get_asset()` (company name lookup) now has a 24h TTL cache; Yahoo/Nasdaq fallback bar-fetching now reuses a pooled session too.
- `scanner_service.py`: news-check `ThreadPoolExecutor` increased from 5→12 concurrent workers.
- **Safety catch**: found the auto-trader unexpectedly `active=true` (leftover from an old persistence test, now with real Alpaca keys live) — turned off, confirmed OFF and persists across restart.
- **Critical bug found+fixed by testing agent**: `tests/test_security_and_trading.py::TestRateLimiting` placed real (paper) BUY orders on every test run with no broker mocking, silently accumulating an AAPL position (1→20→28 shares across sessions). Fixed to use a non-existent ticker (still exercises the rate-limiter, never reaches a real tradable symbol). Liquidated the accumulated 20-share position — account is now flat. All 41 tests still pass.

## Session 8 Update (2026-07-10) — "Find News ASAP" Fix (Warrior Trading Timing)
- **User concern**: news detection was lagging behind the other scanner metrics (price/volume/float flagged first, news confirmation came much later) — problematic for Warrior Trading, where news is usually the root catalyst, not a lagging confirmation.
- **Root cause found**: the scanner checked Google News (slow HTML/RSS scraping, ~1-3s/symbol, ~40s total for 50 candidates under connection-pool contention) as PRIMARY, and only fell back to Alpaca's real-time News API (Benzinga-powered, already included in the existing Alpaca data subscription) if Google News found nothing. The Alpaca fallback itself had a **timezone-naive datetime bug** that made it silently return 0 results every single time — so it never actually rescued anything, and the slow scraper carried 100% of the load.
- **Fix**: flipped priority — `check_alpaca_news()` (new, fixed timezone bug, ~30-200ms/symbol, clean API call) is now checked FIRST; Google News RSS is now the fallback only for illiquid micro-caps Benzinga doesn't cover. Extracted the catalyst-scoring logic (`score_headline`/`classify_freshness`) into shared module-level functions in `google_news_service.py` so both sources apply an identical "real catalyst" quality bar. Also fixed a Google-News connection-pool size mismatch (10 vs 12 worker threads).
- **Concurrency**: volume/float/news second-pass checks now run in 3 parallel threads instead of sequentially (previously: volume → float → news, one after another). Found and fixed a resulting **shared-counter race condition** (all three previously did `criteria_count += 1` on the same dict from different threads) by having each check only write to its own `criteria_met[...]` key, then doing one authoritative `criteria_count` recompute pass after all three finish.
- **Bonus fix**: found and fixed a real (pre-existing, unrelated) race condition in `scan_market()`'s `is_scanning` check-and-set (no lock — two concurrent requests could both bypass the "scan already running" guard and trigger two simultaneous full-market scans, causing Alpaca 429s). Added `threading.Lock`.
- **Result**: real scan latency dropped from ~40-60s to ~6-25s (measured across multiple live scans). This also fixed a previously-flaky pytest test that was timing out due to the old slow news path.
- Testing: 47/47 pytest passing (no flake), 0 criteria_count mismatches across 100 fully-verified scan results in live testing, frontend regression clean. Found + fixed test hygiene bug: `test_manual_process_trigger_no_arity_bug` could leave the auto-trader `active=true` if its assertion failed (no try/finally) — fixed with try/finally, always disables afterward now.

## Session 9 Update (2026-02) — Linux Frontend Systemd Deployment Fix (P0 Blocker Resolved)
- **Bug reported**: on user's live Linux VPS, `momentumx-backend.service` started fine but `momentumx-frontend.service` failed with `status=1/FAILURE`. Previous agent (before this fork) suspected an `EACCES` npm/npx permission error from the low-privilege `momentumx` system user.
- **Actual root cause found+reproduced**: `serve` v14.2.6's `-l`/`--listen` flag changed its parsing — a bare `host:port` string (e.g. `127.0.0.1:4000`, used in the old `ExecStart`) now REQUIRES a URI scheme prefix and throws `Error: Unknown --listen endpoint scheme (protocol): 127.0.0.1:` (exit code 1) otherwise. Reproduced verbatim even as root — confirmed unrelated to file permissions.
- **Fix**: `/app/deploy/linux/momentumx-frontend.service` `ExecStart` changed to `/usr/bin/serve -s build -l tcp://127.0.0.1:4000` (direct binary path, bypassing `npx`; correct `tcp://` scheme). Added `Environment=HOME=/tmp` as defensive hardening (guaranteed-writable dir via `PrivateTmp=true`, protects against any update-notifier cache-write issue for the `--no-create-home` momentumx user under real systemd sandboxing, which can't be fully replicated in this sandbox — no systemd PID1 available here).
- Same `-l host:port` → `-l tcp://host:port` fix applied for consistency to the 3 other places with the identical bug: `/app/deploy/linux/start.sh`, `/app/deploy/windows/install_nssm_service.ps1`, `/app/deploy/windows/start.bat`.
- `install_systemd_services.sh` step [7/7]: added `chown -R momentumx:momentumx "$ROOT_DIR"` after `useradd`, so the service user is guaranteed read access to built frontend/backend files regardless of who ran the installer.
- **Testing**: `testing_agent_v4` validated via bash simulation (no systemd daemon in this sandbox) — reproduced original crash, verified fix resolves it (HTTP 200 as an unprivileged throwaway system user), confirmed all 4 files consistent, confirmed no regression in the running sandbox app. 100% pass, no action items. See `/app/test_reports/iteration_8.json`.

## Session 10 Update (2026-02) — Trading Hours Timezone/Weekday Clarification + Weekend Bug Fix
- User asked whether the 7AM-3:30PM trading window uses their local timezone. Clarified: intentionally hardcoded to `US/Eastern` (not user-local) since the Warrior Trading window is anchored to real NYSE/NASDAQ market hours, which are always defined in ET regardless of trader location.
- **Bug found+fixed while verifying**: `auto_trader_service.is_trading_hours()` and `is_entry_window()` had no weekday check — would treat Saturday/Sunday 7AM-3:30PM ET as valid trading hours (unlike `eod_closer_service.py`, which already correctly skips weekends). Added `now_et.weekday() >= 5` guard to both methods, matching the EOD closer's existing pattern.
- Also investigated user's earlier observation that "scanning only runs when on the page" — confirmed via live backend logs that the real auto-trader engine (`auto_trader_loop()`, 60s interval) already runs fully decoupled from the frontend (started at server boot, gated only by DB-persisted `active` flag) - not a bug. Only the Scanner page's own visual "Auto-Scan" results table is page-scoped (normal SPA component lifecycle), which is a UX nuance, not a functional trading gap.
- Added 4 new regression tests (`TestTradingHours` in `test_auto_trader_exit_logic.py`) covering weekend rejection (Sat/Sun) and weekday acceptance for both `is_trading_hours()`/`is_entry_window()`. Full suite: 20/20 passing in that file.

## Session 11 Update (2026-02) — "7-11 AM" Misleading Label Fix + Trading Hours Display Bug
- User asked why they see an "Outside 7-11 AM ET" warning and confirmed they want entries allowed any time within the full EST trading window. Investigation confirmed: the actual entry logic was ALREADY correct — `is_entry_window()` (7-11 AM restriction) was dead code, never wired into `process_scanner_results()`/`check_entry_signals()`, which only ever gated on `is_trading_hours()` (7 AM-3:30 PM). The bug was purely a misleading UI label.
- **Removed** the unused `is_entry_window()` method entirely (dead code, contradicted actual/desired behavior) and its now-obsolete docstring/comment references to a "7-11 AM entry-only" window in `auto_trader_service.py`.
- **Fixed misleading text** in `Trading.js` ("⏰ Outside 7-11 AM ET" → "Outside Trading Hours (7 AM - 3:30 PM ET)"), `Settings.js` (strategy card + trading hours row), `Scanner.js` (fallback text) to correctly state entries+management run the full 7 AM-3:30 PM ET window.
- **Fixed a separate display bug** found while in this area: `trading_hours` strings in `/auto-trader/status` and `/auto-trader/entry-conditions/{symbol}` rendered as nonsensical `"15:30 PM EST"` (used the 24h end-hour with a "PM" suffix). Now correctly converts to 12h format → `"7:00 AM - 3:30 PM EST"`. Same fix applied to the backend log line in `process_scanner_results()`.
- Removed the corresponding `test_entry_window_false_on_weekend` test (method deleted); kept the 3 weekend/weekday `is_trading_hours()` regression tests from Session 10. 19/19 passing in `test_auto_trader_exit_logic.py`. Verified live via curl: both endpoints now return `"7:00 AM - 3:30 PM EST"`.

## Session 12 Update (2026-02) — Global Scanner Architecture + History Date Bug (Verified)
- **Bug**: Scanner page's Auto-Scan (60s scan loop + notification beep for new 5/5 stocks) was scoped to the Scanner page's own React component lifecycle - it stopped whenever the user navigated to any other page. Dashboard.js also ran its OWN independent, redundant scan-fetch loop.
- **Fix**: Created `/app/frontend/src/hooks/useGlobalScanner.js` - a custom hook called ONCE at the `App.js` root (never unmounts across route changes). It owns autoScan/demoMode/autoTrade/results/criteria/traderStatus state and the 60s interval + new-stock alert beep. `App.js` passes the resulting `scanner` object down as a prop to both `Dashboard` and `Scanner` routes. `Scanner.js` and `Dashboard.js` were refactored to consume this shared prop instead of owning independent state/fetches - eliminates the duplicate parallel scans and makes scanning genuinely app-wide/session-persistent.
- Also lifted `positions`/`recentOrders` fetching from Dashboard-local to a global 30s poller in `App.js` (alongside the existing `account` poller), so this data stays fresh regardless of which page is active.
- **Separate bug found+fixed**: History page's "TODAY" badge on the Daily P&L Tracker assumed the first/most-recent row was always today (`idx === 0`), rather than comparing actual dates - would mislabel e.g. Friday's data as "TODAY" on a later day with no new trades. Fixed with a proper `getETDateKey()` comparison against the real current US/Eastern date (trading days are ET-anchored, so this is the correct timezone to use, not raw UTC or browser-local). Also fixed the underlying naive-timestamp bug in `auto_trader_service.py` (`datetime.now()` → `datetime.now(timezone.utc)` for `entry_time`/`exit_time`/signal `timestamp`) which could shift calendar-date computation depending on browser timezone; confirmed NOT site-wide (isolated to History's daily-grouping + auto-trader source data).
- **Verified via `testing_agent_v4`** (`iteration_10.json`): live 70-second test confirmed a `/api/scanner/scan` background request fired while sitting on Dashboard (not Scanner), scan count incremented from navigating away and back, Dashboard's shared data matched Scanner's exactly. History's TODAY badge correctly applied only to the real current date. 100% pass, no regressions, no action items.
- Trading.js intentionally left with its own page-scoped polling (positions/scanner/momentum every 15-60s) - this only runs while actively on the trading terminal itself, which is the expected/desired behavior for that page (not the "stops when I leave" pattern the user was reporting), and changing it risked regressions in the buy/sell order flow for limited additional benefit.

## Session 13 Update (2026-02) — Email+Password Auth + Live Market Data Split (Verified)
- **Auth overhaul**: Replaced the single static `API_ACCESS_TOKEN` bearer-token gate with proper email+password JWT authentication. Exactly ONE hardcoded user (seeded idempotently on startup from `ADMIN_EMAIL`/`ADMIN_PASSWORD` in `.env`). `auth.py` rewritten: bcrypt password hashing, JWT (30-day expiry), brute-force lockout (5 failed attempts / 15 min, keyed by email only - see below). New unprotected `auth_router` (`POST /api/auth/login`, `GET /api/auth/me`) separate from the JWT-protected `api_router`. Frontend: `TokenGate` replaced with `LoginForm` (email+password fields) - reuses the exact same `setToken()`/localStorage/axios-interceptor mechanism as before, so zero changes needed anywhere else in the app.
- Called `integration_playbook_expert_v2` before implementing (mandatory for any auth change) and adapted the recommended pattern to keep the existing Bearer-token-in-localStorage architecture (rather than switching to httpOnly cookies) since the whole frontend already funnels through one shared axios interceptor expecting this exact pattern - avoided a much larger, riskier refactor for no added benefit in this single-user internal tool.
- **Live market data / paper trading split**: Added `ALPACA_DATA_API_KEY`/`ALPACA_DATA_SECRET_KEY` (a separate LIVE Alpaca account) used ONLY for read-only market data (`StockHistoricalDataClient`) and news (`NewsClient`) in `alpaca_service.py` and `scanner_service.py`. `TradingClient` (all actual order execution) is UNCHANGED and still exclusively uses the original PAPER keys (`ALPACA_API_KEY`/`ALPACA_SECRET_KEY`, `paper=True`) - triple-verified via code review, direct Python inspection, and live `/api/account` calls confirming `account_number=PA30RVV1A2DM` (paper) throughout.
- **Real bug found+fixed while wiring this up**: `get_bars()` was explicitly passing `end=datetime.now()` to Alpaca's `StockBarsRequest`, which causes Alpaca's free-tier data plan to hard-reject the ENTIRE request with "subscription does not permit querying recent SIP data" - even though older data in the same range was available. Removing the explicit `end` param (letting Alpaca auto-clamp to whatever it's allowed to serve) fixed this system-wide: confirmed AAPL bars now return `source: "alpaca_iex"` directly from Alpaca instead of always falling back to Yahoo. This was the deeper root cause behind the user's earlier "limited chart data" question - not just an account-tier limitation, but an explicit-end-param bug compounding it.
- **Verified via `testing_agent_v4`** (`iteration_11.json`): login flow (wrong creds error, correct creds success, JWT persists across refresh, old token fully rejected), safety-critical paper-account check, and market-data-source fix all confirmed working in the live browser. One MEDIUM issue found (brute-force lockout unreliable through the ingress proxy due to inconsistent `get_remote_address()` IP detection) - fixed by switching the lockout identifier to email-only (no IP component; appropriate for a single-user tool), re-verified via curl through the public URL (5 fails → 429 on 6th attempt, reliably). Full pytest suite: 59/59 passing after updating 2 test files to use JWT login instead of the removed static token.
- ⚠️ Security note given to user: the live Alpaca keys were shared in plaintext chat - recommended rotating them in the Alpaca dashboard once things are stable (same guidance given for the paper secret key earlier this session).

## Session 14 Update (2026-02) — Chart Lag Bug Fix (Verified)
- **Bug**: User reported significant lag in 1-minute/5-minute charts, suspecting the new live Alpaca account wasn't being used properly for data.
- **Root cause**: `get_bars_with_fallback()` in `alpaca_service.py` only validated Alpaca's bar data via price-proximity to the live quote (within 5%) - it never checked the bar's actual timestamp age. Alpaca's free-tier data plan embargoes the most recent ~15-19 minutes of intraday bars (confirmed via direct testing - a genuine third-party platform limitation, not fixable without a paid Alpaca data subscription). For a low-volatility stock, a 15-19 minute old bar can easily stay within 5% of the current price, so stale data was passing the price check and being displayed as if current.
- **Fix**: Added an explicit timestamp-based staleness check for intraday timeframes (1Min/5Min) - if Alpaca's latest bar is more than 3 minutes old, skip Alpaca entirely and use the Yahoo Finance fallback (which appends a live-quote-based synthetic bar, giving near-zero lag). Daily (1Day) bars are unaffected by this check (not subject to the same real-time embargo issue).
- **Verified via `testing_agent_v4`** (`iteration_12.json`): confirmed both via backend curl (AAPL/TSLA/MSFT, all showing sub-1-minute freshness vs previously 15-19 min stale) and live frontend Network inspection on real scanner tickers (RIBBU, FGI - both showing ~0.35-0.39 min staleness). 100% pass, no regressions, no action items.

## Deferred / Backlog (P1/P2)
- Consider splitting `server.py` (1280+ lines) into per-domain routers (orders/settings/auto-trader/market) for maintainability (noted by testing agent, non-blocking).
- Docker Desktop / `docker-compose.yml` path was NOT built (user chose Windows-native/NSSM) — available on request if preference changes later.
- Pre-existing scanner performance issue (found this session, not fixed - separate scope): `_check_candidate_news` (Google News scraping) can take ~40s under connection-pool contention with news.google.com, occasionally causing `/auto-trader/process` to exceed the ingress gateway timeout (502). Real float lookup (this session's fix) only adds ~0.1-2s, confirmed via logs — not the bottleneck. Candidate future fix: swap/augment Google News scraping with a proper news API, or increase gateway timeout.
- Optional (still open, not actioned): LLM-based news-catalyst sentiment classification to replace the current keyword-match approach — bigger scope, needs a new LLM key integration.

## Session 7 Update (2026-07-10) — Warrior Trading Review + Real Float Data Fix
- **CRITICAL FIX**: The scanner's "Float < 20M shares" criterion (one of the 5 required Warrior Trading pillars) was silently using `random.randint()` fake data every time the optional Interactive Brokers float service wasn't connected (which it always was, in this setup) — directly violating the "no fake data" mandate from Phase 2 and corrupting the strategy's stock-selection quality. Root-caused during a Ross Cameron strategy review requested by the user.
- Fix: added `alpaca_service.get_float_data()` — real shares-outstanding data from **SEC EDGAR** (`data.sec.gov`, free, no API key, real company filings via `dei/EntityCommonStockSharesOutstanding` with `us-gaap/CommonStockSharesOutstanding` fallback), 24h TTL cache, ticker→CIK mapping cached once/24h. `scanner_service._calculate_accurate_float()` runs this in parallel (10 workers) for the top 50 candidates, same pattern as the existing volume/news second-pass verification. If no real data is found (unknown ticker, no filings), the float criterion is marked **unmet** (fail-safe) — never guessed. IB float service (if ever connected) still takes priority as source #1.
- Verified real & consistent (not random) via two consecutive scans returning identical `shares_outstanding` for the same symbols; added regression test `tests/test_float_and_analytics.py::TestScannerRealFloat`.
- Frontend: Scanner.js Float column now shows "N/A" (not misleading "0.0M") when float data is unknown.
- Updated stale PDT (Pattern Day Trader) messaging in `Dashboard.js` ("Account Leverage" card) — SEC/FINRA eliminated the classic $25k-minimum/3-day-trades-per-5-days PDT rule in 2026 (effective June 2026, broker rollout phases in through Oct 2027), replaced by real-time intraday margin monitoring. Old absolute claim removed.
- **P1 backlog closed**: bounded `trade_history`/`missed_opportunities` analytics Mongo queries — `get_analytics()` now filters by date at the query level (default last 180 days, `days=0`/`None` for all-time) instead of loading up to 10,000 raw docs into memory. New `?days=` query param on both `/trade-history/analytics` and `/missed-opportunities/analytics`.
- **P2 backlog closed**: kill-switch notification — `App.js` polls `/auto-trader/status` every 30s app-wide (any page) and fires a `sonner` toast (edge-triggered, once per true→false transition) when the daily-loss/consecutive-loss kill switch halts trading. Toast-only per user's choice (no email integration).
- Testing: 47/47 backend pytest passing (41 previous + 6 new), full frontend regression clean (Dashboard/Scanner/Trading/History/Missed/Settings/Demo). One pre-existing minor React key-prop warning on History page noted (cosmetic, unrelated to this session).

## Session 15 Update (2026-02) — Custom-Directory Linux Install Fix (Verified)
- **Bug**: user cloned the repo to `/opt/Internal-trader` (not the documented `/opt/momentumx` default). `install_systemd_services.sh` copies two static `.service` templates that hardcode `/opt/momentumx` in `WorkingDirectory`/`PATH`/`ExecStart` — services would have pointed at a nonexistent directory and failed to start.
- **Fix** (implemented previous session, verified this session): script now computes `ROOT_DIR` from its own real location (`SCRIPT_DIR`/`BASH_SOURCE`), and after copying the `.service` files to `/etc/systemd/system/`, runs `sed -i "s|/opt/momentumx|$ROOT_DIR|g"` on both — only when `$ROOT_DIR != /opt/momentumx` (true no-op for the documented default path).
- **Verified via `testing_agent_v4`** (`iteration_13.json`): simulated both the custom-dir case (`/tmp/test_install/Internal-trader` — all 4 path references across both unit files correctly rewritten, 0 remaining `/opt/momentumx` refs) and the default-path regression case (sed skipped, files byte-identical to source templates). `bash -n` syntax check clean. 100% pass, no code action items.
- Applied the one non-blocking doc suggestion: `deploy/linux/README.md` step 1 updated to state any clone directory under `/opt` works (installer auto-patches paths) instead of implying `/opt/momentumx` is a hard requirement.
- User can now safely run `sudo ./install_systemd_services.sh` from `/opt/Internal-trader/deploy/linux` — see reinstall instructions given in-chat (git pull → run installer → `systemctl status` to verify → SSH tunnel to access).

## Session 17 Update (2026-07) — Paper↔Live Trading Toggle + Chart Data Pipeline Fix (Both Verified)

### Paper ↔ Live Trading Mode Toggle (NEW feature, user-requested)
- User: "We should have a toggle for changing between paper and prod trading based on the keys" — the live Alpaca key pair (`ALPACA_DATA_API_KEY`/`SECRET`) was previously data/news-only; now usable for real order execution too, gated behind an explicit toggle.
- Confirmed design: Settings page UI toggle (instant switch, "GO LIVE" typed-confirmation modal required only when switching TO live), auto-trader permanently blocked from running in LIVE mode (manual orders only, no unattended bot with real money), risk limits unchanged between modes, persistent red "LIVE — REAL MONEY AT RISK" banner app-wide when live.
- **Where it lives**: Settings page → "Trading Mode" toggle (Paper/Live switch). Switching to live requires typing "GO LIVE" in a confirmation dialog. A red banner appears at the top of every page + the nav badge turns "🔴 LIVE" whenever active.
- Backend: `alpaca_service.py` now holds two separate `TradingClient` instances (`_paper_client` always `paper=True` on `ALPACA_API_KEY`, `_live_client` always `paper=False` on `ALPACA_DATA_API_KEY`, only "available" if genuinely a different key pair). `self.trading_client` is an alias reassigned by `set_trading_mode()`. Mode persists in `db.app_config._id='trading_mode'`, restored on startup (defaults to paper if never set). Auto-trader force-disabled on entering live mode and on startup if it somehow restored active+live together.
- New endpoints: `GET/POST /api/trading-mode`. Guard added to `POST /api/auto-trader/toggle` (400 if enabling while live) and the `auto_trader_loop()` background task (skips processing while live, defense-in-depth).
- **Verified via `testing_agent_v4`** (`iteration_15.json`): 100% pass — confirm-text gating (case-sensitive "GO LIVE"), auto-trader block while live, account_number actually differs between modes (proves real order routing switch, not cosmetic), restart persistence, banner/badge across all pages, environment left in PAPER mode as required.

### Chart Data Pipeline Fix (user-reported bug + perf request)
- User: "I do not see a proper chart... prod API should be faster... start on 1M charts... last 2 days... asap for next candles... each chart a single call."
- Root causes found + fixed:
  1. `Trading.js` useEffect re-triggered a FULL heavy chart reload (4 parallel requests, 1170+234+30 bars) for EVERY open chart on EVERY unrelated background poll (scanner ~60s/momentum ~120s/positions ~15s) — fixed with a `loadedChartsRef` guard so each chart's full history loads exactly once per open (cleared on close, so reopening re-fetches fresh).
  2. `alpaca_service.get_bars()` computed the intraday lookback window using literal elapsed minutes, not calendar days — a 780-bar "2 day" request only looked back 13 hours regardless of weekends/market-closure. Rewrote to pad the start_date by calendar days (accounting for ~390/78 bars per trading day + weekend/holiday buffer) and trim server-side to the most recent N bars.
  3. `StockChartCard.js` defaulted to 5Min; changed default to 1Min.
  4. Incremental refresh tightened from 30s→15s for faster new-candle visibility; fixed a latent bug where the first incremental update silently trimmed the just-loaded 2-day window back to 1 day (trim caps now match the fetch window: 780/156).
  5. Fixed a stale-closure bug (found by testing agent) where the 15s interval closed over `selectedStocks`/`positions`/etc. from mount time — now reads from refs kept in sync via a small effect.
  6. Also fixed test-isolation flakiness in the pytest suite (`auto_trader.active` race between `test_security_and_trading.py` and `test_trading_mode_toggle.py` under xdist parallelism) via module-level `xdist_group` pinning + retry-resilient test cleanup.
- Note for user: chart data source may still show `yahoo` instead of `alpaca` for intraday bars — this is expected; Alpaca's free-tier IEX feed embargoes the most recent ~15 min regardless of which key pair (paper or live) is used, so the app correctly falls back to Yahoo for freshness. A paid Alpaca "Algo Trader Plus" data subscription on the live account would be needed to get real-time SIP data directly from Alpaca instead.
- **Verified via `testing_agent_v4`** (`iteration_16.json`, plus a follow-up fix + reverification for the stale-closure bug it found): chart opens in 1Min by default, renders real data, each open chart issues exactly one initial burst + lightweight 15s incremental calls (no more redundant full reloads), bar counts scale correctly with requested limit. Full pytest suite: 83/83 passing (excluding the pre-existing, documented, load-induced 502 flake on `/auto-trader/process` under heavy repeated stress-testing, self-heals, not a regression).

## Session 16 Update (2026-02) — "First Pullback" Strategy Correction (Verified)
- User provided a full Ross Cameron Warrior Trading class transcript on the exact "First Pullback" entry pattern and explicitly requested the LIVE auto-trader logic be corrected to match ("This is how we need to be trading so we can do it properly"), not just documentation.
- **4 real gaps found + fixed** in `auto_trader_service.py`:
  1. Pullback candle color was inverted — the code treated the pullback as 1-3 GREEN candles; the real pattern is 1-3 RED candles (profit-taking) after a surge, then a GREEN breakout candle that breaks the high of the immediately preceding red candle. Rewrote `check_micro_pullback()` → `check_first_pullback()` with correct red-candle detection + breakout-trigger logic.
  2. **The 50% Rule was completely missing** — added: the pullback must hold at least `pullback_retracement_max_pct` (default 50%) of the initial surge, or the setup is discarded as "too weak".
  3. **Stop-loss was a flat 1% trailing %** instead of the structural "low of the pullback" Ross Cameron actually uses. Now `stop_loss_price` = the real pullback low, with a `max_stop_distance_pct` (default 3%) safety cap that skips the trade entirely if the structural stop is too far from entry (too risky/low-conviction). Profit target is now a TRUE 2:1 reward:risk off the real structural risk (`entry + 2*(entry-stop)`), not a flat 2%.
  4. **No "breakout or bailout" time-stop existed** — added: if a fresh entry hasn't moved into profit within `breakout_bailout_seconds` (default 90s), `monitor_exits()` now exits immediately rather than waiting for the full stop to hit.
- New settings (`pullback_retracement_max_pct`, `max_stop_distance_pct`, `breakout_bailout_seconds`) exposed via `POST /api/auto-trader/settings` and `GET /api/auto-trader/status`. Updated `entry-conditions/{symbol}` label/detail text to red-candle terminology.
- Created `/app/backend/tests/test_first_pullback_strategy.py` (12 new unit tests covering pattern detection, the 50% rule, breakout-trigger requirement, structural stop, 2:1 target formula, safety cap, bailout timing).
- Created `/app/WARRIOR_TRADING_FIRST_PULLBACK_STRATEGY.md` — full rule-by-rule breakdown with 4 diagrams (the user's originally-provided screenshot URLs were dead/404, so accurate replacement diagrams were AI-generated) and the complete lesson transcript. Updated the older `/app/WARRIOR_TRADING_STRATEGY.md` quick-reference (fixed several stale "7-11 AM" references left over from Session 11, updated to the new structural-stop/2:1-target/bailout language). Updated `Settings.js` strategy info card copy to match.
- **Verified via `testing_agent_v4`** (`iteration_14.json`): 70/71 pytest passing (1 pre-existing unrelated flaky 502 on `/auto-trader/process` under live multi-symbol fetch load, self-heals, not caused by this change). Live API verification against real market data (AAPL/TSLA/MSFT/NVDA) confirmed correct new fields/labels/text. Settings persist/reset round-trip confirmed. Frontend Settings page renders the new copy correctly, no console errors. `auto_trader.active` confirmed OFF throughout — no real/paper orders were placed during verification.

## Session 3 Update (2026-07-10) — Real Alpaca Paper Verification
- User provided real Alpaca paper credentials (key `PKRBZGHKVX2SGHWQZZVRJLXRPN`, account `PA30RVV1A2DM`). Configured in `backend/.env`.
- Found + fixed a real bug during first live call: `alpaca_service.get_account()` crashed with `float() argument ... NoneType` because this paper account's `daytrading_buying_power`/`pattern_day_trader` fields come back as `None` from Alpaca (cash/non-margin account) — now defaults gracefully to `0.0`/`False` instead of crashing.
- Verified end-to-end with a real order round trip: BUY 1 AAPL → real fill price ($313.07) → position auto-added to monitor with real stop config → real buying-power deduction → SELL 1 AAPL → real trade-history entry logged with real P&L. Real market bars confirmed via Yahoo fallback (Alpaca free-tier IEX data was stale) — no fake data anywhere.
- Fixed one stale test assertion in `test_security_and_trading.py` that hardcoded an empty-API-key expectation; now environment-agnostic. All 41 tests still pass.


## Session 2 Update (2026-07-10) — Phase 4 & 5

### Phase 4 — Logic Quality/Trust ✅
- **#13 Reconciled strategy params** with Ross Cameron's documented Warrior Trading rules (user's explicit choice): kept code's 10% position size / 2% profit target / 1% stop loss (already a correct 2:1 R:R), changed `daily_max_loss_pct` from 5% → **1%** (Ross's "conservative starting" daily-loss rule) as a hard kill switch. Fixed all stale UI text (`Settings.js` info card) and `WARRIOR_TRADING_STRATEGY.md` doc to match the reconciled values exactly (was showing stale 5%/10%/5%/10%/11 AM).
- **check_micro_pullback wired in** as a REQUIRED entry condition (`require_micro_pullback=True`) — was previously computed but unused (diagnostic-only). Now gates real auto-trader entries in `check_entry_signals()`, checked first (after 5/5 scanner criteria, before volume/MACD/SMA).
- **#15 Extended-hours slippage guard**: tightened the market-order limit-price buffer from ±10% → ±3%, and added a hard reject when bid/ask spread > 8% (protects against illiquid $2-$20 low-float names in extended hours).
- **Bonus bug fix**: `POST /api/auto-trader/settings` was silently broken (AttributeError referencing non-existent `pullback_min_pct`/`pullback_max_pct`) — fixed to use the real attribute names and added `require_micro_pullback` to the response.
- **#14 (real fill prices for P&L)** was already implemented during the Phase 1-3 pass (server.py sell logging, auto_trader monitor_exits, position_monitor partial/stop/bearish-exit paths all already read `filled_avg_price` when available).

### Phase 5 — Windows-VPS Deployment (Windows-native, per user choice — no Docker) ✅
- `/app/deploy/windows/README.md` — full step-by-step guide (prereqs, `.env` setup, build, run)
- `/app/deploy/windows/start.bat` — one-touch manual startup (MongoDB + backend + frontend, 3 windows)
- `/app/deploy/windows/install_nssm_service.ps1` / `uninstall_nssm_service.ps1` — persistent Windows services via NSSM (auto-start, survives reboot/RDP logout), backend bound to `127.0.0.1` only (RDP-only network model, no internet exposure)
- `backend/.env.example`, `frontend/.env.example` — deployment templates
- Root `/app/README.md` rewritten with security summary + link to deploy guide

### Testing (Session 2)
- Testing agent independently re-verified: 41/41 pytest pass, auto-trader/status reflects new params, settings bug fix confirmed, code-review of micro-pullback gating + slippage guard ordering, Settings.js UI text reconciled, all 7 frontend pages smoke-tested, logout regression re-confirmed. Zero bugs found (1 cosmetic stale code comment, fixed).

## Session 18 Update (2026-02) — Real-Time Alpaca WebSocket Market Data Streaming (Verified)
- **User concern**: REST polling for market data (bars/quotes) is subject to Alpaca's free-tier ~15-minute embargo on recent data, which was ruining the auto-trader's ability to time "First Pullback" entries/exits accurately ("otherwise how would the python know when to take trades?"). Called `integration_playbook_expert_v2` for the Alpaca WebSocket streaming playbook before implementing (mandatory for any 3rd-party integration).
- **New backend service** `backend/services/market_data_stream_service.py` — `MarketDataStreamManager`: persistent `asyncio` background task maintaining a WebSocket connection to `wss://stream.data.alpaca.markets/v2/iex`, authenticated with the LIVE/data key pair (`ALPACA_DATA_API_KEY`/`SECRET`, same keys already used for REST market data) regardless of the paper/live trading-mode toggle (per user's explicit confirmation: "Definitely the Prod key regardless of setting"). Features: auto-reconnect with exponential backoff + re-subscribe on reconnect, `ping_interval`/`ping_timeout` heartbeat, in-memory `latest_quotes`/`latest_trades`/rolling 1-min `bar_buffers` caches, `merge_with_stream()` (dedupes REST bars + stream bars by UTC-minute bucket to fill exactly the free-tier's ~15min gap without discarding real REST history), trade/quote subscriptions capped at 25 symbols (Alpaca free-tier limit; minute-bar subscriptions are unlimited).
- **New WebSocket endpoint** `WS /api/ws/market-data` (unprotected `ws_router` — browsers can't set `Authorization` headers on a WS handshake, so JWT is passed as `?token=` and verified manually; rejects with close code 4401 if missing/invalid). Client sends `{"action":"subscribe","symbols":[...]}`; server forwards only messages for that client's subscribed symbols.
- **REST endpoints upgraded** to prefer/merge stream data: `GET /api/market/quote/{symbol}` and `/market/quotes` now serve from the <8s-fresh stream cache when available (falling back to REST); `GET /api/market/bars/{symbol}` (1Min/5Min) and `GET /api/auto-trader/entry-conditions/{symbol}` now merge the REST fallback-chain result with `merge_with_stream()`. New `GET /api/market/stream-status` (debug/health).
- **Core fix for algorithm accuracy**: `auto_trader_service._get_real_bars()` (used by every live entry-signal check) now also merges stream bars — the auto-trader's pullback/MACD/SMA pattern detection now sees genuinely real-time candles for the most recent ~15 minutes instead of REST's stale embargo window. Every symbol checked (scanner candidates, entry-condition lookups, chart opens) is auto-subscribed to the stream as a side effect, so coverage grows organically through the trading day.
- **Frontend**: new hook `frontend/src/hooks/useMarketDataSocket.js` (auto-reconnecting WS client, exposes `{quotes, trades, bars, connected, subscribe}`). `Trading.js` subscribes the live stream to all visible symbols (open charts + positions + top scanner/momentum candidates), merges live quote ticks straight into `stockData` for instant bid/ask updates, and shows a `data-testid="stream-status-indicator"` badge ("🟢 Live stream connected" / "Reconnecting...") in the action bar.
- **Safety preserved**: live-mode auto-trader force-disable rule is completely unrelated code path, unaffected by this change — reconfirmed via testing.
- **Verified via `testing_agent_v4`** (`iteration_17.json`): 16/16 backend tests passing (1 skipped — live-mode-specific, app currently in paper mode), new `backend/tests/test_market_data_stream.py` covering stream-status shape/growth, quote/bars/entry-conditions integration, WS token accept/reject, auto-trader safety non-regression. Frontend: login, "Live stream connected" indicator, Settings GO LIVE modal all confirmed working. Live curl verification: `authenticated: true` against real Alpaca IEX feed, subscription counts increase correctly, 1Min bars endpoint returns REST+stream-merged data with a live `realtime: true` tail bar. Minor non-blocking notes: benign `asyncio` task-exception log noise during dev-server hot-reload only (not steady-state), and a pre-existing (unrelated) scanner concurrency quirk.

## Session 19 Update (2026-02) — Removed Yahoo/Nasdaq Fallback, Alpaca-Only Real Data (Verified)
- **User**: "We shouldn't be using a fallback for yahoo. That was for testing. We need real data for the charts from the prod API." Root cause of the request: the WebSocket streaming feature (Session 18) already made the Yahoo/Nasdaq fallback obsolete — the free-tier REST embargo gap it was compensating for is now filled by the real Alpaca stream (`merge_with_stream()`), so mixing in a second data provider was leftover from before that fix existed.
- **Removed entirely** from `alpaca_service.py`: `get_bars_yahoo()`, `get_bars_nasdaq()`, and the price/staleness-based branching in `get_bars_with_fallback()` that decided when to switch providers. `get_bars_with_fallback()` now: tries Alpaca's real bars only → if empty/failed, falls back to a single real-time Alpaca quote point (`source: realtime_quote_only`, `no_historical_data: true`) → if that also fails, explicit `source: none` / "NO DATA AVAILABLE". Never touches a non-Alpaca provider.
- **Found+fixed a second direct Yahoo call** in `position_monitor_service.py::_check_bearish_crossover_exit()` (SMA20/50 bearish-crossover early-exit check) — was calling `get_bars_yahoo()` directly, bypassing the fallback chain entirely. Now uses `get_bars_with_fallback()` + `market_data_stream.merge_with_stream()`, consistent with every other bar-fetching call site.
- Updated `tests/test_security_and_trading.py::test_market_bars_real_source` allowed-source assertion (removed `yahoo`/`nasdaq`, added `realtime_quote_only`).
- **Verified**: live curl confirmed `source: alpaca_iex` for AAPL/TSLA 5Min bars (real OHLCV, not synthetic), invalid symbol correctly returns explicit `no_historical_data: true` instead of ever trying Yahoo. Full pytest suite (`test_security_and_trading.py` + `test_market_data_stream.py`, 47 tests): 43 passed, 4 initially failed due to a **transient real Alpaca REST 429 "too many requests" rate-limit spike** (from cumulative heavy testing load this session, not caused by this change) — re-ran the affected tests after a short cooldown and all passed cleanly.
- **Operational note surfaced to user**: Alpaca's REST API does rate-limit under heavy concurrent load (seen in both this environment's logs and the user's own VPS logs: `"too many requests"` during scanner batch scans). Now that Yahoo is removed, a rate-limited moment will show an explicit "no data" on affected symbols rather than silently substituting Yahoo data — this is the correct/intended tradeoff per the user's request (real prod data or nothing), not a regression. Not yet addressed: no retry/backoff on Alpaca 429s specifically (separate, not-yet-requested potential follow-up).
- Also fixed (Session 18 follow-up, same conversation): this preview/dev sandbox and the user's production VPS share the same `ALPACA_DATA_API_KEY`, and Alpaca only allows ONE active streaming WebSocket connection per key pair — the preview holding that slot was causing `406 connection limit exceeded` / `401 not authenticated` loops on the VPS. Fixed by adding a `MARKET_STREAM_ENABLED` env toggle (`market_data_stream_service.py`) and setting it to `false` in this preview's `.env` only (VPS defaults to enabled/unset = true, unaffected).

## Session 20 Update (2026-02) — Scanner Rate-Limit/Connection-Pool Fix (Verified)
- **User reported**: live VPS logs showing constant `"too many requests"` errors during scans + `Connection pool is full, discarding connection: data.alpaca.markets` warnings, worried it was caused by the Session 19 Yahoo-removal change. Confirmed via code trace: **unrelated, pre-existing bug** in `scanner_service.py`'s own full-universe scan (128 batches × 100 symbols, 10 concurrent workers hammering Alpaca's snapshot endpoint) — nothing to do with chart-bars fallback. Root cause confirmed via web search: Alpaca's free/Basic Market Data API caps at **200 requests/minute**; 128 rapid snapshot requests plus concurrent volume/news calls easily exceeds that, and the SDK's fixed 3-retry/3s-wait isn't enough to smooth out a rate that's structurally too high for 20-40+ seconds straight. The connection-pool warning is a second symptom of the same root cause: all Alpaca SDK clients share one `requests.Session()` with the default urllib3 `pool_maxsize=10`, exactly saturated by 10 concurrent threads.
- **Fix** (`alpaca_service.py`): added a shared, process-wide `AlpacaRateLimiter` (sliding 60s window, capped at 170 req/min — leaves headroom under Alpaca's real 200/min limit for other concurrent Alpaca calls) and a `mount_larger_connection_pool()` helper (pool_maxsize 10→20). Applied `alpaca_rate_limiter.acquire()` before every Market Data REST call (`get_bars`, `get_quotes`, `get_latest_quote`) and mounted the larger pool on `alpaca_service`'s own `data_client`.
- **`scanner_service.py`**: imports the same shared limiter/pool-mounter (so the scanner's *separate* `StockHistoricalDataClient`/`NewsClient` instances correctly share the *same* real Alpaca account-level quota as `alpaca_service.py`'s calls, not their own independent budget). Mounted the larger pool on `data_client`/`news_client`. Reduced the main batch-scan `ThreadPoolExecutor` from 10→6 workers. Added rate-limiter-gated retry-with-backoff (2 retries, 5s/10s) directly in `process_batch()` so a transient 429 retries instead of silently dropping that batch's candidates. Also gated `check_alpaca_news()`'s and `_calculate_accurate_volume()`'s Alpaca calls through the same limiter.
- **Verified via live scan** (curl-triggered full scan): **zero** "too many requests" errors and **zero** "connection pool full" warnings across the entire 128-batch scan (previously dozens of each) — 108 candidates found with 0 dropped batches (vs. previously variable/lossy). Full `pytest tests/test_security_and_trading.py::TestRealDataOnly` still passing.
- **Trade-off disclosed to user**: properly respecting the rate limit means a full scan now takes ~60-65s (previously ~20-40s, but with silently dropped/incomplete batches). This is the necessary cost of "always real, never rate-limit-violating" data, consistent with the user's Session 19 request. Not a regression risk on the user's actual VPS (direct SSH tunnel to uvicorn, no reverse-proxy gateway timeout) — only observed as an occasional curl-timeout artifact in this preview sandbox's own ingress, which has its own unrelated timeout.
- **Bonus bug found+fixed while verifying**: `alpaca_service.place_market_order()`'s extended-hours branch crashed with `'NoneType' object has no attribute 'get'` (opaque 500) whenever `get_latest_quote()` returned `None` (e.g. an invalid/delisted symbol with no quote data) — pre-existing, unrelated to today's changes, just newly surfaced by direct testing. Fixed with an explicit `if not quote: raise Exception(...)` guard producing a clean, readable error message instead of a raw attribute-error traceback.
- **Full pytest suite re-run** (99 tests): 3 residual failures, all traced to the SAME already-disclosed trade-off — full scans (~64s) now exceed this **preview sandbox's own ~60s ingress gateway timeout** more consistently than before (502), specifically on endpoints that synchronously await a full scan (`/api/auto-trader/process`, back-to-back `/api/scanner/scan` calls in `test_real_scan_float_data_consistent`). Confirmed via isolated re-run that the rate limiter itself works correctly (20/min order limit correctly returns 429 in isolation) — the full-suite 500s were xdist concurrent-load flakiness, not a logic bug. This gateway-timeout pattern is preview-environment-only (already documented pre-existing item #6 in the backlog) and does not affect the user's real VPS (no reverse proxy in front of uvicorn there).

## Session 21 Update (2026-02) — Alpaca/Benzinga News Check Was Silently 100% Broken (Fixed + Verified)
- **User noticed**: scanner logs only ever showed `google_news_service` hits, never a single "Alpaca/Benzinga found" line, despite the code explicitly checking Alpaca's Benzinga-powered news API FIRST.
- **Root cause**: `scanner_service.check_alpaca_news()` accessed `news_set.news` on the alpaca-py SDK's `NewsSet` response model — that attribute **does not exist** on the installed SDK version (confirmed via direct inspection: articles actually live under `news_set.data['news']`). `hasattr(news_set, 'news')` was always `False`, so the method **always** silently returned `{'has_news': False, 'articles': []}` regardless of what Alpaca actually had — for every symbol, every scan, 100% of the time — forcing complete reliance on the slower Google News RSS-scraping fallback. No exception was ever raised (so nothing appeared in logs even at debug level), which is exactly why this went unnoticed.
- **Fix**: read articles from `news_set.data.get('news', [])` instead of the nonexistent `.news` attribute. Also bumped the exception-path log from `logger.debug` (invisible at the app's configured `INFO` log level) to `logger.warning`, so any *future* Alpaca News API failures are actually visible in production logs instead of silently vanishing again.
- **Verified live** (not simulated): pulled Alpaca's raw current news feed, found real strong-catalyst headlines (e.g. "Lionsgate Studios attracts takeover interest..." for `LION`, a shareholder-approved acquisition for `PSA`), confirmed `check_alpaca_news()` now correctly returns `has_news: True` with the right scored article for both — proving the Alpaca/Benzinga path genuinely works end-to-end now. (The specific candidates in the reported scan — SPAC tickers like RIBBU/GSRFR/EGHAU, and non-catalyst headlines for AMDD/JCTC — legitimately have no Benzinga catalyst coverage or don't pass the catalyst-scoring bar; that part was always correct, Google News fallback existing precisely for that gap.)
- **New regression test**: `backend/tests/test_alpaca_news.py` (3 tests, all passing) — asserts the response shape, and pulls a live catalyst headline from Alpaca's raw feed each run to prove `check_alpaca_news()` actually surfaces it (this exact test would have caught the original bug immediately).

## Session 22 Update (2026-02) — One-Screen Manual-Review Dashboard Rebuild (Verified)
- **User request**: replace the old Dashboard.js (basic account cards) with a dense, one-screen manual-review trading dashboard inspired by (not copying) a multi-panel scanner-terminal reference screenshot. Explicit choices: (1) flame emoji news-freshness badges (red=breaking/orange=warm/yellow=cold) directly on scanner rows, (2) large trade prints as a real-data proxy for order-flow support/resistance (true Level 2 depth isn't available on Alpaca's free tier - user confirmed this is fine), (3) panels = scanner table + dedicated "5/5 Ready to Trade" alert panel + a 2x2 chart grid (10Sec/1Min/5Min/1Day) for one symbol + a news feed, (4) charts only populate on manual row click (no auto-focus). Used `design_agent_full_stack` for the visual blueprint (`/app/design_guidelines.json`), and referenced Alpaca's official docs (News API, streaming market data) the user linked to validate the implementation against the real API contract.
- **Backend additions** (`market_data_stream_service.py`): `trade_ticks` rolling buffer + `get_tick_bars()` (constructs genuinely-real 10-second OHLCV bars from raw Alpaca trade ticks — Alpaca's Bars API has no "seconds" timeframe at all, so this is the only way to get sub-minute charts without fabricating data); `get_large_trades()` (tick-rule buy/sell tagged large block-trade prints, dynamic threshold = max(500 shares, 5x rolling average size) — the real-data proxy for order-flow support/resistance the user agreed to). New endpoints: `GET /api/market/large-trades/{symbol}`, `GET /api/market/bars/{symbol}?timeframe=10Sec`. Fixed `GET /api/news/{symbol}` to use the Alpaca-first pipeline (was Google-News-only before, inconsistent with the scanner's own pipeline).
- **Frontend**: new `pages/Dashboard.js` orchestrator + `components/dashboard/` (AccountStrip with an inline scan-toggle + live stream indicator, ScannerTable with flame badges + criteria dots, ReadyToTradePanel, ChartGrid/MiniChartTile, NewsFeedPanel, ScannerCells shared cell components). Extended `CandlestickChart.js` with a `blockTrades` prop rendering large-trade levels as dotted buy(green)/sell(red) horizontal lines.
- **Verified via `testing_agent_v4`** (`iteration_18.json`): found and I fixed ONE critical regression before it verified clean — a copy/paste artifact left `merge_with_stream()`'s `def` line missing (its body was dead code sitting unreachable inside `get_large_trades()`), breaking 1Min/5Min chart tiles with a raw exception leaking into the UI. Fixed (one-line restore), re-verified live via curl (`source: alpaca_iex` real bars) and a new regression test suite `backend/tests/test_dashboard_market_endpoints.py` (10/10 passing). Confirmed working end-to-end: scan-toggle → real ~65s scan → 108 rows with real price/%/RVol/float/criteria-dot data, flame badges rendering correctly on rows with real news (RIBBU/GSRFR/EGHAU), row click sets header + highlight + correctly switches chart grid/news feed with no stale-data leak, 1Day chart renders real candles+SMA20/50+VWAP, 10Sec tile gracefully shows "no ticks yet" (expected - this preview's `MARKET_STREAM_ENABLED=false`, doesn't affect the user's VPS). No regressions in Trading/Settings/History/Missed pages.
- **Scope note for user**: MACD sub-panes and EMA9 (visible on ONE panel in the reference screenshot) were intentionally left out of the compact 2x2 grid tiles to avoid clutter at that size — current tiles show candles + volume + SMA20/SMA50 + VWAP + block-trade lines. Could add a larger single-chart "detail view" with full MACD/EMA later if wanted.

## Session 23 Update (2026-07) — Dashboard Selection-Persistence Fix + Quick Trade Panel + Local Bars Caching (Verified)
- **P0 fix (carried over from previous fork's in-progress item)**: Dashboard losing the selected symbol's focus/highlight during periodic refreshes. Root cause traced to `ScannerTable`'s `results` array being fully replaced by each ~60s auto-scan cycle, filtering OUT any symbol that no longer matches the live scan criteria - the selected row would silently vanish from the table (not a React state bug; `selectedSymbol` itself was always correctly preserved). Fix: `Dashboard.js` now keeps a `pinnedRow` snapshot of the selected symbol's last-known scanner data and synthesizes a `_stale: true` row (rendered at `opacity-50` with a "stale" badge in `ScannerTable.js`) whenever the live symbol has dropped out of `scanner.results`, so the selected row/chart focus is NEVER lost while the user has it open.
- **Completed the incomplete live-tick wiring** noted in the previous fork's handoff: `MiniChartTile.js` was receiving `liveTrade` but never forwarding it to `CandlestickChart` as `livePrice` (dead prop) - now wired through, plus the tile's price label now prefers the live WS tick over the last REST bar close.
- **New feature (user-reported gap)**: "There's not a buy option on the dashboard." Added `components/dashboard/QuickTradePanel.js` - a one-click BUY/SELL panel next to the selected-symbol header, reusing the exact same localStorage risk-setting keys (`dollarAmountPerStock`/`stopLossPct`/`takeProfitPct`/`stopType`/`trailingStopPct`/`partialSellPct`/`partialSellTrigger`/`moveToBreakeven`) already used by `Trading.js`, so orders behave identically regardless of entry point. Qty auto-calculated from `dollarAmountPerStock`/current price (editable), SELL button only renders when a matching open position exists (full-qty close). `App.js` passes a new `onOrderPlaced` callback into the Dashboard route to refresh account/positions immediately after a quick trade.
- **New feature (user-requested perf)**: local bars caching so periodic refreshes don't re-pull full historical windows. Backend: `GET /api/market/bars/{symbol}` now accepts an optional `since=<ISO timestamp>` param; `alpaca_service.get_bars()`/`get_bars_with_fallback()` use it to override the start-date window, returning only NEW bars instead of the full `limit`-sized history. Frontend: new `utils/barsCache.js` (in-memory, per symbol+timeframe) lets `MiniChartTile.js` instantly repaint from cache on symbol re-selection (no blank "Loading..." flash) and pass `since=<last cached bar timestamp>` on every 30s refresh, merging the small incremental response into the cached array by timestamp.
- **Bonus fix found by testing agent**: `POST /api/orders`' outer `except Exception` was re-wrapping intentional `HTTPException`s (e.g. 400 "Market is closed") as 500s. Added `except HTTPException: raise` before the generic handler - now correctly returns 400/403 for intentional rejections. Curl-verified.
- User explicitly declined the earlier-proposed "LIVE ORDER" safety badge task this session (backlog item removed).
- `MARKET_STREAM_ENABLED` was briefly flipped to `true` to investigate (immediately hit Alpaca's real 406 "connection limit exceeded" since the user's VPS holds the only allowed stream slot) and reverted back to `false` right away - confirmed still `false` at session end, as required.
- **Verified via `testing_agent_v4`** (`iteration_19.json`): 100% pass on all 3 new/fixed features - selection persistence with stale-badge fallback confirmed across a 60s wait spanning both the 30s account poll and a 60s auto-scan cycle; QuickTradePanel renders, qty auto-calculates, BUY correctly fires `POST /api/orders` and surfaces a clean toast (success or the expected "Market is closed" error outside 4am-8pm ET, both handled cleanly); bars caching confirmed via network interception - instant repaint on re-selection + `since=` param present on all refresh-cycle requests. Full regression pass (login, all 7 pages, Trading page buy/sell) clean, zero console errors.

## Session 24 Update (2026-07) — VPS Duplicate-Clone Diagnosis + Resizable Panels + $/% Position Sizing (Verified)
- **User support issue diagnosed (not a code bug)**: user's self-hosted VPS didn't show Session 23's changes after `git pull` + `yarn build`. Root cause found: user had TWO separate clones on disk (`~/autotrader` where they were pulling/building, and `/opt/autotrader` where the actual `momentumx-frontend`/`momentumx-backend` systemd services' `WorkingDirectory` pointed) - confirmed via `systemctl cat momentumx-frontend`. Git commit hash on the VPS (`58a3292`) exactly matched this sandbox's HEAD, proving the pull itself worked fine; the build/rebuild was just happening in the wrong directory. Gave user diagnostic commands (`find / -iname autotrader -type d`, `systemctl cat <service> | grep WorkingDirectory`) to catch this class of issue proactively in the future, plus a fix (`git pull`/`yarn build`/restart in `/opt/autotrader`, or symlink the two paths together).
- **New feature: draggable, persisted resizable panels** (user: "I need to be able to adjust the sizing on these windows or tiles... may not always have the same size screen" - explicitly chose draggable handles with saved sizes over fixed presets/pure-responsive reflow). `Dashboard.js`'s 3 main columns (scanner/chart-grid/news) and `ChartGrid.js`'s 2x2 tile grid (10Sec/1Min/5Min/1Day) both rewritten from CSS grid to nested `ResizablePanelGroup`/`ResizablePanel`/`ResizableHandle` (shadcn wrapper around the already-installed `react-resizable-panels` v3 library), each with a distinct `autoSaveId` so drag positions persist across reloads via localStorage (`react-resizable-panels:dashboard-main-columns`, `-chart-grid-rows`, `-chart-grid-row1-cols`, `-chart-grid-row2-cols`).
- **New feature: $ vs % position sizing** (user: "I should be able to change the purchase amount percentage of my full account" - explicitly chose to ADD BOTH $ flat and % modes, control living in the same place as the other manual-trade risk settings i.e. Trading.js's inline "Trade Settings" row, not the separate Settings.js page). Added `positionSizeMode`/`positionSizePct` localStorage-backed state + a `getPerStockDollarAmount()` helper in `Trading.js` (used by both `placeOrder`/`buyAllSelected`), with a new $/% toggle UI in the Trade Settings row (live "≈$X" preview computed from the real account `portfolio_value`). `Trading.js` now receives `account` as a prop from `App.js` (previously had none). `Dashboard.js`'s `QuickTradePanel` reads the exact same localStorage keys + now also receives `account`, so both manual-buy entry points always agree on sizing mode/amount.
- **Verified via `testing_agent_v4`** (`iteration_20.json`): 100% pass - resize handles (5 total: 2 main-column dividers + 1 chart-grid row splitter + 2 per-row column splitters) drag correctly, localStorage layout updates live, and a real `page.reload()` (F5) confirmed sizes genuinely persist (not just in-memory). Position sizing toggle verified end-to-end on both Trading.js (≈$44 preview for 10%/$438.54, ≈$88 for 20% - correct math) and Dashboard QuickTradePanel (qty auto-calculated correctly in both percent and dollar modes for different symbols). One low-severity finding (fresh-session `dollarAmountPerStock` default mismatch: 2000 in Trading.js vs 100 in QuickTradePanel.js) - fixed same session, now both default to 2000.

## Session 25 Update (2026-07) — Sortable Scanner Headers + All-Symbol Live Pricing + Full-Day Chart Bars + Admin Login Docs Fix (Verified)
- **`.env.example` fix**: was missing `JWT_SECRET`/`ADMIN_EMAIL`/`ADMIN_PASSWORD` entirely (auth silently doesn't work without them - backend logs a startup warning). Root-caused a user login issue on their VPS to this gap. Added the 3 keys with instructions; auth's `seed_user()` (`auth.py`) is idempotent - editing `ADMIN_PASSWORD` in `.env` + restarting the backend rotates the login password any time.
- **New: live price ticking on the scanner table itself** (user: "the data isn't updating very fast between scans for pricing"). Previously the WS feed only subscribed to the single selected/charted symbol - the whole left-side scanner table sat frozen at whatever the last full `~60s` scan returned. `Dashboard.js` now subscribes the WS feed to ALL currently-visible scanner symbols and a new `withLivePrices()` helper overlays each row's live WS trade price + recomputes `pct_change` from that row's `prev_close` (same reference the backend scan itself uses) - both `ScannerTable` and `ReadyToTradePanel` now receive this live-priced data.
- **New: sortable scanner table column headers** (user: "I do not see an option to sort... by any of the headers... should let me sort based on any of the metrics"). `ScannerTable.js` - all 6 columns (Sym/Price/Chg%/RVol/Float/5-5) are now clickable, toggle ascending/descending with a chevron indicator (lucide-react), defaults to the original criteria-count-then-volume ranking when no column is actively selected.
- **New: full trading-day chart bar counts** (user: "it should be a full day for each chart, not just what fits on the screen"). `MiniChartTile.js` previously requested a flat `limit=100` bars for every timeframe, which only covered ~16 min on the 10Sec chart or ~100 min on 1Min - nowhere near a full session. Added a `FULL_DAY_BAR_LIMIT` map (1Min: 960, 5Min: 200, 10Sec: 600, matching the app's own 4am-8pm ET 16-hour extended-hours window) and fixed the incremental cache-merge call to trim to that same per-timeframe limit instead of a hardcoded 100. Curl-verified backend now returns the full counts (960/200 bars respectively). SMA20/50 indicators are now meaningfully populated across the full session as a side benefit.
- **MARKET_STREAM_ENABLED troubleshooting**: user asked to verify live data on the preview; confirmed their VPS is the one actively holding Alpaca's single allowed stream connection (406 "connection limit exceeded" when toggled on here, twice, at user's request). Per user's final instruction, left `MARKET_STREAM_ENABLED="false"` in this sandbox - genuine live ticking on the 10Sec tile and the ~15-min REST-embargo-gap-fill on 1Min/5Min can only be verified on the VPS where the real connection lives. User is aware and will flag when to revisit.
- **Verified via `testing_agent_v4`** (`iteration_21.json`): 100% pass on sortable headers (all 6 columns, both directions, correct cell-value ordering, chevron indicators) and the all-symbols WS subscribe wiring (captured actual outgoing WS frame containing all visible symbols, not just selected). Full-day bar limits self-tested via curl (confirmed exact bar counts returned) + one smoke-test screenshot (charts render correctly with denser data, SMA lines populated, zero console errors) - not sent to the full testing agent since it's a contained parameter-tuning change with no new interactive surface.

## Session 26 Update (2026-07) — Trade-Only Sound Alerts + Sell 1/2 / Sell All on Dashboard (Verified end-to-end with real paper orders)
- **Sound alert scope change** (user: "We should only be making sounds when trades are made", after finding the previous new-candidate beep too noisy/confusing). Removed the beep-on-new-scanner-candidate logic entirely from `useGlobalScanner.js` (was firing on every ~60s scan whenever any new symbol appeared/disappeared from results - far too frequent to be a useful signal). New shared `utils/sound.js` (`playTradeSound()`) is now called ONLY on an actual completed trade: manual buy/sell from `Trading.js` (`placeOrder`, `buyAllSelected`, sell-all-positions), the Dashboard's `QuickTradePanel.js` buy/sell, and auto-trader entries/exits (detected in `useGlobalScanner.js` by diffing the auto-trader's open position symbols before/after each `/api/auto-trader/process` call - a genuine trade fires the sound, a no-op cycle stays silent).
- **New: Sell 1/2 / Sell All on the Dashboard** (user: "I need a sell 1/2 or all option on the stocks I'm in on the Dashboard"). `QuickTradePanel.js`'s single "SELL {qty}" button replaced with two buttons - "SELL 1/2" (rounds to nearest share, disabled if position qty < 2) and "SELL ALL {qty}" - both call the same `/api/orders` sell flow with the appropriate quantity.
- **Verified end-to-end with real paper orders** (market was open during testing, regular session): bought a real 4-share test position, clicked "SELL 1/2" on the Dashboard - confirmed via toast ("Sold 2 JEM @ $5.98") AND a follow-up `GET /api/positions` call that exactly 2 shares sold, 2 remained. Cleaned up both test positions (JEM, SOFI) via sell-all afterward, account back to $0 positions.
- **Found while testing (documented, not fixed - out of scope for this session)**: the backend scanner cache (`scanner_service.py`) is NOT keyed by criteria - a request with different filter criteria within the ~120s cache window can incorrectly get served an older request's cached result. Also, `alpaca_service.get_position_entry_time()` uses the wrong Alpaca enum (`OrderStatus.FILLED` instead of `QueryOrderStatus`) causing a caught-and-logged (non-blocking) Pydantic validation error on every sell - doesn't affect the actual order, just fails to look up entry time silently.

## Session 27 Update (2026-07) — Fixed Uncapped % Position Sizing Bug (Verified)
- **Bug**: user reported a failed buy - `Buy RIBBU failed: {"buying_power":"438.3",...,"cost_basis":"4274.59","message":"insufficient buying power"}`. Root cause: the "% of account" position-sizing input (Session 24) only had HTML `min`/`max` attributes, which are visual hints only, not enforced on typed/programmatic values - a stored value like `975` (instead of `10`) would compute `975% of $438 ≈ $4270` worth of shares, ~10x the account.
- **Fix (3 layers)**: (1) `Trading.js`'s percent input `onChange` now hard-clamps to `Math.min(100, Math.max(1, value))`. (2) The `positionSizePct` `useState` initializer self-heals any previously-corrupted localStorage value back into range on load. (3) Defensive clamp added directly inside both `Trading.js`'s `getPerStockDollarAmount()` and `QuickTradePanel.js`'s inline calc, so a stray/corrupted stored value can never again size an order beyond the account's value regardless of how it got there.
- **Verified via screenshot**: simulated the exact corrupted state (`positionSizePct='975'` in localStorage) - confirmed it self-heals to `100` on page load with a sane `≈$436` preview, and typing `500` directly into the input also clamps live to `100`.

## Session 28 Update (2026-07) — Fixed Stale/Dormant Ticker Data Bug (Verified)
- **Bug**: user asked "are we seeing relative volume properly? Something can have a 25% change and 0.0x relative volume - RIBBU specifically." Investigated end-to-end with direct Alpaca SDK calls: RIBBU's `latest_trade.timestamp` was `2026-06-22` - over 3 weeks stale. Alpaca's snapshot endpoint keeps serving the LAST trade it ever recorded for illiquid/dormant tickers (common for SPAC "unit" tickers, often barely trade) even when nothing has traded in weeks. The scanner was computing `pct_change` from this stale carried-over print against an equally-old prior close, producing a misleading "25% move" that never happened today - meanwhile `volume_ratio` was CORRECTLY near-zero (there's genuinely no volume today), which is what tipped off the user that something was inconsistent.
- **Fix**: `scanner_service.py`'s `_process_stock()` now checks `snapshot.latest_trade.timestamp` against today's US/Eastern date before processing a symbol at all - if the most recent trade isn't from today's session, the symbol is skipped entirely (every downstream metric for a dormant ticker would be equally meaningless, not just volume).
- **Verified**: direct Alpaca SDK query confirmed RIBBU's stale timestamp; re-ran a full live scan post-fix and confirmed RIBBU no longer appears (42 results vs 82 before, at a different time of day so not a clean apples-to-apples count, but explicitly confirmed absent) while genuinely fresh (if thin) movers like AEHG/ELVA/TSEG - spot-checked to have today's timestamps - remain correctly included with their real (low) volume ratios intact.

## Session 29 Update (2026-07) — MAJOR FIX: Feed Mismatch Was Suppressing Relative Volume ~20-50x Across the Board (Verified)
- **User follow-up**: "Is this correct? RTB $16.00 +10.3% 0.0x 4/5 ... Rel Vol < 5x" - RTB's `latest_trade` WAS from today (unlike RIBBU), so this needed a deeper look.
- **Root cause found**: `scanner_service.py`'s `_calculate_accurate_volume()` fetches 20 days of historical daily bars via `StockBarsRequest` with NO explicit `feed` parameter, while the live snapshot's `daily_bar.volume` (current_volume) implicitly comes back on the **IEX** feed (the free-tier default for current/today data). Direct side-by-side Alpaca SDK queries at the identical instant proved the bars request without a feed param was silently returning **full SIP-consolidated** volume (RTB: 57,603 shares) while the snapshot's IEX-only current volume was 1,241 - a ~46x mismatch for the SAME symbol/day. Since IEX represents only a few percent of true consolidated market volume, this made `avg_volume` (the ratio's denominator) 20-50x too large for basically EVERY stock scanned, making the 5x relative-volume threshold nearly unreachable even on genuinely high-volume days.
- **Fix**: added `feed=DataFeed.IEX` explicitly to both the `StockSnapshotRequest` and the historical `StockBarsRequest` in `scanner_service.py`, so current_volume and avg_volume are now computed from the same feed basis (like-for-like comparison).
- **Verified live**: re-ran the scan after the fix - RTB's volume_ratio went from a meaningless `0.0x`/`~0.03` to an accurate `0.66x` (avg_volume corrected from a SIP-inflated number down to a real ~3,189 IEX shares) - still correctly below the 5x threshold given its genuinely modest volume, but now a TRUE number. Meanwhile previously-suppressed genuine high-volume movers now show correctly: AEHG 9.3x, ELVA 10.0x, ERNA 29.9x, WAI 20.3x (all were showing near-zero, e.g. 0.04x-0.17x, before this fix). Confirmed on the live Dashboard UI via screenshot - a healthy spread of RVol values (0.3x-29.9x) instead of everything compressed near zero.
- This is likely the single highest-impact fix of the project to date for the core "First Pullback" strategy's accuracy, since relative volume ≥5x is one of the 5 required criteria and was previously nearly impossible to satisfy for real setups due to this feed mismatch.

## Session 30 Update (2026-07) — API Speed Tuning + Dedup Scanning + Sell-Anywhere Bug Fix + Risk:Reward Auto-Target (Verified)
- **Log review / speed investigation** (user shared production `journalctl` logs, asked "see any issues so far?" then "are we close to the limit for speed"). Looked up Alpaca's official docs directly (not from training data) - confirmed user is on the **Basic** plan: 200 req/min market-data ceiling (separate bucket from the Trading API's own 200/min), 30 WS symbol subscriptions, 1 concurrent stream connection, and historical bars cannot cover the latest 15 minutes (this is *why* the app stitches live IEX + historical SIP for chart volume - a hard Basic-plan constraint, not a bug; confirmed with user to leave as-is unless they upgrade to Algo Trader Plus).
- **Fix 1 - local resource caps raised** (`alpaca_service.py`): shared `AlpacaRateLimiter` 170→190/min (still under Basic's real 200/min ceiling with a small safety buffer); `mount_larger_connection_pool` default 20→60 (eliminates the "Connection pool is full, discarding connection" warning churn seen in the logs - this was a purely local urllib3 resource cap, unrelated to Alpaca's real rate limit).
- **Fix 2 - eliminated duplicate full scans** (found via code trace, not the logs alone): `auto_trader_loop()` (60s background loop) and `/api/auto-trader/process` were both calling `scanner_service.scan_stocks()` **directly**, bypassing the existing 120s cache+lock wrapper (`scan_market()`) that the main `/api/scanner/scan` endpoint already used - meaning every auto-trader cycle ran its own full duplicate 128-batch snapshot scan even when the dashboard had just done an identical one seconds earlier. Both call sites switched to `scan_market()`. Confirmed (by tracing `check_entry_signals()` → `_get_real_bars()`) that actual entry-price/timing decisions always re-fetch fresh live bars per-candidate regardless of scan-cache age - only the candidate shortlist gets the (already-accepted-elsewhere) 120s staleness tolerance, so trading correctness is unaffected.
- **Bug fix - no way to sell a position not currently charted** (user: "I do not see a sell or sell all button on the dashboard... anywhere that we can buy"). Root cause: `QuickTradePanel`'s Sell buttons only appear for whichever single symbol is currently selected/charted, and there was no way to select a symbol that had scrolled off the scanner table (still held, no longer matching scan criteria) - meaning a held position could become permanently un-sellable from the Dashboard. New `OpenPositionsPanel.js` - an always-visible strip below `AccountStrip` listing every open position with inline "1/2" / "SELL" buttons per row plus a global "SELL ALL (n)" button, fully independent of chart selection.
- **New feature - Risk:Reward-driven profit target + trailing-stop default** (user: "profit targets set once I buy based on the risk reward ratio we have set... trailing stop as the default option"). `Trading.js`: added a `riskRewardRatio` setting (default 2.0, localStorage `riskRewardRatio`) with a `useEffect` that recomputes `takeProfitPct = stopLossPct * riskRewardRatio` any time either changes - the old independently-editable "Target %" input is now a read-only computed display. Changing the ratio also pushes `POST /api/auto-trader/settings {reward_risk_ratio}` so the algorithmic auto-trader stays in sync. `stopType` default changed `'fixed'` → `'trailing'` in both `Trading.js` and `QuickTradePanel.js` (only affects fresh/never-configured localStorage - explicit past user choices are preserved). Backend: `auto_trader_service.py` gained `self.reward_risk_ratio` (default 2.0), used in `check_entry_signals()`'s structural-stop target-price formula (was hardcoded `2 * risk_per_share`, now `self.reward_risk_ratio * risk_per_share`), settable via `update_settings()`, exposed on `/api/auto-trader/status` and `/api/auto-trader/settings`.
- **Verified via `testing_agent_v4`** (`iteration_22.json`): 100% pass, backend 8/8 pytest (new `test_rr_sell_smoke.py`) + full live Playwright run with a real paper NIO position opened and fully closed via both the new sell-half and sell-all buttons. R:R input confirmed recalculating Target live (Stop=2/RR=2→Target=4.0, RR=3→Target=6.0) and firing the settings sync POST; `/api/auto-trader/status` reflected the last-set ratio; Stop Type confirmed defaulting to "Trailing" on a fresh browser context; QuickTradePanel BUY confirmed sending `stop_type:'trailing'` by default. No regressions found in the scan_market dedup change.
- **Bug fix - missing stop/target/trailing chart lines** (user: "When I buy I do not see the stop loss or take profit lines... should all show on the charts site wide"). `CandlestickChart.js` already supported a `levels` prop (pre-existing, used on the Trading page's `StockChartCard`) but the Dashboard's `MiniChartTile`/`ChartGrid` never passed it. `Dashboard.js` now computes `levels` (entry/stopLoss/profitTarget/trailingStop) from the same shared localStorage settings, anchored to the real position `avg_entry_price` once a position is open (not the fluctuating current price), threaded through `ChartGrid` → `MiniChartTile` → `CandlestickChart`. Verified via `testing_agent_v4` (`iteration_23.json`) with a real paper ATPC buy - lines confirmed anchoring exactly to the fill price. **Found but not fixed** (pre-existing, unrelated): Trading.js's multi-chart comparison view (`StockChartCard`) is stuck permanently on "Loading {symbol}..." and never renders, even though the underlying fetch succeeds - needs its own RCA session, user has not yet confirmed priority.
- **New strategy: HOD-retest / psychological-level / topping-tail take-profit overhaul** (user shared detailed Ross Cameron video notes). See below for full detail - implemented in `auto_trader_service.py`, verified via `testing_agent_v4` (`iteration_24.json`).
- **Dashboard layout rework** (user: "charts snap to the right side... news under the stocks, 50/50 on the left sidebar"). See below for full detail - verified via `testing_agent_v4` (`iteration_25.json`).

## Session 30 Continued — HOD-Retest Strategy Overhaul + Dashboard Layout Rework (Verified)
- **New auto-trader take-profit strategy** (user shared detailed notes from a Ross Cameron momentum-trading video): `auto_trader_service.py` overhauled to use a 3-tier take-profit system instead of a flat reward:risk %.
  - **Target A (HOD retest)**: uses the already-computed `surge_peak` from `check_first_pullback()` (the peak of the surge right before the pullback being traded) as the primary profit target. The `reward_risk_ratio` setting is now a **quality filter on entry** - if the HOD retest isn't at least `reward_risk_ratio x risk` away, the trade is rejected outright ("setup not high quality enough"), not just given a smaller target.
  - **Target B (psychological level)**: new `_nearest_psych_level()` helper finds the nearest whole/half-dollar level above entry, minus a 2-cent buffer (e.g. $2.50 → $2.48), used as an interim partial-profit trigger below Target A.
  - **Topping tail**: new real-time check in `monitor_exits()` - fetches the latest 1-min bar via `_get_real_bars()`, flags a "topping tail" when in profit and the upper wick (`high - close`) is ≥ `topping_tail_wick_ratio` (default 0.5) of the bar's range. Acts as an independent early-exit trigger alongside the price targets.
  - **Partial vs full exit behavior** (per explicit user confirmation): if `enable_partial_profit` (new toggle, default True) is on, the first trigger (Target B or an early topping tail) takes a partial sell (existing `partial_sell_pct`) and moves the stop to `breakeven_buffer_pct` (new, default 0.2%) **above** entry (not just exactly breakeven) - then the runner exits on Target A or a topping tail on the remainder. If off, ANY trigger (Target A, Target B, or topping tail) closes the full position immediately.
  - Time-stop/"breakout or bailout" (exit at ~breakeven if no follow-through within `breakout_bailout_seconds`) already existed pre-session and needed no changes.
  - New settings (`enable_partial_profit`, `breakeven_buffer_pct`, `topping_tail_wick_ratio`) exposed on `/api/auto-trader/status` and settable via `/api/auto-trader/settings`, no dedicated frontend UI built yet (backend/algorithm-only per user's request).
  - Verified via `testing_agent_v4` (`iteration_24.json`): 6/6 new pytest tests pass, settings round-trip correctly, no crashes in entry-conditions/scanner/toggle endpoints across a real ~90s auto-trader-active window.
- **Dashboard layout rework** (user: "charts snap to the right side... news under the stocks, 50/50 on the left sidebar"). `Dashboard.js` changed from a 3-column layout (scanner | charts | news) to 2 columns: **left sidebar** (~32%, resizable) with a nested vertical split - Stocks (ReadyToTradePanel + ScannerTable) on top, News on bottom, 50/50, independently resizable; **right side** (~68%, resizable) - QuickTradePanel header + 4-tile ChartGrid, using the freed-up space for a full 16:9 canvas. Verified via `testing_agent_v4` (`iteration_25.json`): both drag handles work, full selection→chart→news→quicktrade data flow intact after the move, no wasted whitespace at 1920x1080.
- **Minor findings, not fixed this session (flagged only, out of scope/pre-existing)**: scanner occasionally exceeds the ~60s ingress timeout under heavy Alpaca rate-limit backoff (502 to client, scan itself completes fine server-side); a `position_monitor_service` "insufficient qty available" race observed once against a pending order; STOP/TRAIL chart price-line labels can visually crowd when stop % is very close to entry %.

## Session 30 Continued — Resizable Panel ID-Collision Fix + Full-Width Dashboard + Explained Live-Bar Constraint
- **Resize/snap bug fix** (user: "charts are not resizable as I would expect and are not staying snapped to the right when new data comes in"). Root cause: `Dashboard.js` and `ChartGrid.js` had 5 nested `react-resizable-panels` groups with no explicit `id` props - two of them (`ChartGrid`'s row1-cols/row2-cols) are structurally identical parallel trees, so their auto-generated fallback ids collided, corrupting resize/persistence behavior. Fixed by adding explicit unique `id`/`order` props to every `ResizablePanelGroup`/`ResizablePanel` in both files. Verified via `testing_agent_v4` (`iteration_26.json`) with live drag tests: row1/row2 tiles now resize fully independently, and sizes persist byte-for-byte across a 13s data-refresh cycle (previously would drift/snap).
- **Full-width Dashboard** (user: "I should have the whole 16x9 space... it doesnt need a buffer on the sides"). `App.js`'s `<main>` now renders without the `container mx-auto` max-width/padding specifically on the Dashboard route (`/`), while every other page keeps its normal centered/padded layout. Initial fix read `window.location.pathname` directly in `App` (outside Router context), which didn't update reactively on client-side nav clicks (found + fixed same session, `iteration_26.json` → `iteration_27.json`) - resolved by extracting a `MainContent` child component that uses `useLocation()` properly.
- **Investigated "10 second data sometimes disappears" / "live data for the bars"** - NOT a code bug. `server.py`'s `/api/market/bars` endpoint serves 10Sec bars exclusively from `market_data_stream.get_tick_bars()` (built live from WS ticks, zero historical/REST fallback exists for seconds-level granularity). Since `MARKET_STREAM_ENABLED=false` in this preview sandbox (intentional - the user's production VPS holds the single allowed Alpaca stream connection), there's no live tick stream here at all, so the 10Sec tile and true tick-level bar movement between polls can only be genuinely verified on the production VPS, not in this preview. Communicated clearly to user; no environment override attempted (would violate the single-connection constraint).

## Session 31 Update (2026-07) — Margin Buying Power Sizing + WS Priority/Eviction + Chart Auto-Snap-to-Live-Edge (Verified)
- **Margin trading fix** (user: "we are using margin trading always at the max"). Root cause: `auto_trader_service.calculate_position_size()` was always sized off `account.portfolio_value` (unlevered equity), never `buying_power`/`day_trading_buying_power` (Alpaca's margin-extended figures) - so trades never used more than 1x unlevered equity regardless of actual account margin. Fixed: `alpaca_service.get_account()` now returns a new `margin_buying_power = max(buying_power, day_trading_buying_power)` field; all 3 `server.py` call sites (`/auto-trader/status`, `/auto-trader/process`, `auto_trader_loop()`) now feed this (falling back to `portfolio_value` if missing/zero) into `process_scanner_results()`/`calculate_position_size()` instead of raw equity. **Verified but numerically inert on the current test account right now**: this paper account's equity ($460) is below Alpaca's Reg-T margin minimum, so Alpaca itself reports `day_trading_buying_power=$0`/`buying_power==portfolio_value` - margin_buying_power correctly equals equity today, but the code path is now correctly wired to scale up automatically the moment Alpaca extends real margin (larger paper balance, or PDT-qualified live account).
- **"10-second chart never loads" root-caused**: `market_data_stream_service.subscribe()`'s 25-symbol trade/quote cap (Alpaca free-tier hard limit) was strictly FIFO/first-come with no eviction - since `Dashboard.js` subscribes the WHOLE scanner results list (~50-90 symbols) on every scan tick, the currently-selected/charted symbol could permanently lose the race for a tick-data slot to whatever scanner candidates streamed in first, so its 10Sec/1Min tiles never received real ticks. Fixed: `subscribe(symbols, priority=False)` now supports eviction - a `priority=True` symbol always gets a slot, evicting the oldest non-priority (FIFO via new `_tq_order`/`_tq_priority` tracking) if the cap is full. Wired `priority=True` at every "this symbol is actively in use" call site: the selected chart symbol + open positions (`Dashboard.js` → WS `subscribe(symbols, priority)` → `/api/ws/market-data` handler), the `/api/market/bars/{symbol}` (10Sec + 1Min/5Min), `/api/market/quote/{symbol}`, `/api/market/large-trades/{symbol}` REST side-effect subscribes, server startup's existing-positions subscribe, and `position_monitor_service.monitor_positions()`'s 2s loop (re-asserts priority for ALL live Alpaca positions every tick, regardless of how they were opened - the most robust safety net). Scanner-wide/background candidate subscribes (`auto_trader_loop`) stay non-priority as before.
- **10-second refresh cadence fix**: `MiniChartTile.js` polled ALL 4 tiles (10Sec/1Min/5Min/1Day) on the same 30s cadence - meaning the "10-second" tile only ever got a new candle every 30s. Added a separate `FAST_REFRESH_MS=3000` used only for the `10Sec` tile; the other 3 tiles keep their existing 30s cadence (an explicit prior user preference, intentionally unchanged).
- **Chart auto-snap-to-newest-bar UX fix** (user: "charts are not snapping to the right most bar as new ones come in... when I zoom in and out we should still be snapped to the right"). Root cause: `CandlestickChart.js` tracked a one-way `userHasZoomedRef` flag that, once set by ANY zoom/pan (including the library's own internal adjustments), permanently forced every future data refresh to restore the exact OLD visible range via `setVisibleRange(savedRange)` - new bars arriving at the right edge were never shown without a manual drag. Rewrote using `lightweight-charts` v4's real-time-follow primitives: `isFollowingRef` (true = auto-snap) is derived from `chart.timeScale().scrollPosition()` on `subscribeVisibleLogicalRangeChange` - `pos < -1.5` (deliberately panned back into history) turns following off; otherwise (at/returning-to the live edge, including right after a zoom) it calls `scrollToRealTime()` (preserves current zoom/bar-spacing, just re-anchors the window to the newest bar) both immediately and again on every data refresh while following.
- **Bug found+fixed by testing agent mid-session** (`iteration_28.json`): the above handler reacted synchronously to every INTERMEDIATE range-change event during an active drag (before the pan had crossed the -1.5 "into history" threshold), calling `scrollToRealTime()` mid-gesture and fighting/distorting the user's own drag into a wrong, wider zoomed-out end state. Fixed by debouncing the handler (`rangeChangeDebounceRef`, 200ms) - only evaluates `scrollPosition()`/decides follow-vs-not once a drag/zoom gesture has fully settled, never mid-gesture. Re-verified via `testing_agent_v4` (`iteration_29.json`): the reported distortion was NOT reproduced after the fix (fast drag-into-history correctly preserved zoom and stayed panned back after a 5s settle; zoom-then-stay-at-live-edge confirmed stable). One inconclusive sub-case (a slow multi-step drag test) reverted to the live edge instead of staying panned back but showed none of the original distortion pattern - testing agent attributed this to test-coordinate precision (narrow mini-chart tiles with left-side label overlays eating into drag space) rather than a real regression; `retest_needed: false`.
- **Verified via `testing_agent_v4`**: backend `iteration_28.json` - 12/12 new pytest tests passing (`backend/tests/test_margin_and_stream_priority.py`) covering `margin_buying_power` field correctness, all 3 server.py call sites, stream-status shape, priority marking on 10Sec/1Min bar fetches, bulk-fill eviction staying under the 25-symbol cap with no crash. Frontend `iteration_28.json`/`iteration_29.json` - full regression (login/scanner/ChartGrid/positions) clean, zero console errors, 10Sec 3s-refresh confirmed no infinite-loop/excessive-rerender, chart zoom/pan/snap behavior confirmed working after the debounce fix.

## Session 31b Update (2026-07) — Fixed Misleading Margin Label, %-of-Account Sizing Bug, Dual-Key Data Speed Boost, CRITICAL Stop-Loss Retry-Loop Bug (Verified)
- **"(2x)" header label was hardcoded, not real**: `App.js` always displayed "Buying Power (2x)"/"(4x)" based only on `pattern_day_trader` status, regardless of what Alpaca actually extended. Fixed to compute the real ratio (`buying_power / portfolio_value`) - this surfaced the actual root cause of the user's "I still don't see margin enabled" report: their paper account's equity was below Alpaca's $2,000 Reg-T margin minimum, so Alpaca itself was giving $0 real margin (not a code bug).
- **Alpaca key swap**: user rotated `ALPACA_API_KEY`/`SECRET` (paper trading) to a new account (`PA36RNHPHRUZ`, real 4x margin - $18k+ buying power vs ~$4.5k equity, confirming the Session 31 margin-sizing fix is now numerically live). The OLD paper key was repurposed (not discarded) - see below.
- **"% of account not targeting buying power" bug, found+fixed**: the Session 31 margin fix only covered the AUTO-TRADER's backend sizing - the separate MANUAL "% of account" quick-trade sizing mode (`Trading.js` `getPerStockDollarAmount()` and `QuickTradePanel.js`) was still computing off unlevered `portfolio_value`. Both now use `margin_buying_power` (falling back to `buying_power` then `portfolio_value`).
- **Dual-key market-data speed boost** (user's request: repurpose the OLD paper key "as a way to increase the speed of information only"). Confirmed via investigation: Alpaca's News/Bars/Quotes/Snapshot APIs were ALL already hard-tied to a single "prod" data-account key (`ALPACA_DATA_API_KEY`) with one shared ~190/min rate limiter - this was the actual scan/news/chart speed bottleneck (the OLD trading key never touched data/news at all, confirmed - it was trading-only). Since Alpaca enforces data rate limits PER-ACCOUNT, added a new `AlpacaClientPool` (`alpaca_service.py`) that round-robins every Bars/Quotes/Snapshot/News request across BOTH the primary data account AND the user's old paper key (now added as `ALPACA_SECONDARY_DATA_API_KEY`/`SECRET`, data/news-only, never used for trading), each with its own independent rate limiter - roughly doubling combined throughput. `scanner_service.py` no longer builds its own single-account clients; it draws from the same shared `data_pool`/`news_pool` singletons. Verified: both keys independently confirmed live/valid via direct Alpaca calls, round-robin alternation confirmed via direct test (2 distinct clients, 2 distinct keys), scanner/bars/quotes endpoints all pass with the new pooled clients.
- **CRITICAL bug found+fixed mid-testing** (`iteration_30.json`): `position_monitor_service.py` had NO de-dup check before submitting exit orders - if an earlier sell order for a position was still resting/unfilled, the 2s monitor loop would blindly resubmit a full-qty sell every single cycle, and Alpaca would reject every attempt with "insufficient qty available" FOREVER (observed live: 9+ minutes of continuous rejection, a real losing position that could never actually stop out). Root-caused to the exact same underlying enum bug already flagged in the Session 30 backlog (`alpaca_service.get_orders()` used `OrderStatus` instead of `QueryOrderStatus` for the status filter, so checking for open orders threw a `ValueError` rather than working). Fixed: `get_orders()` now uses `QueryOrderStatus` (with a safe fallback to `ALL` for any invalid string - also fixes the minor `GET /api/orders?status=open/new` 422/500 DX issue from the same test pass); new `get_open_orders(symbol)` helper; new `position_monitor_service._sell_with_dedup()` used by all 3 exit paths (`_execute_stop_loss`, `_handle_partial_take_profit`, `_check_bearish_crossover_exit`) - skips resubmission if a sell order for the symbol is <10s old (lets it fill), cancels+replaces if ≥10s old (stale/stuck), places directly if none exists.
- **Verified via `testing_agent_v4`**: `iteration_30.json` (%-of-account fix + dual-key pool: both confirmed correct, 6/6 new backend tests, 0 frontend console errors) and `iteration_31.json` (stop-loss dedup fix: 11/11 new deterministic pytest tests covering fresh-pending/stale-pending/no-order/exception-degrades-gracefully/buy-orders-ignored cases, plus full 152-test regression suite at 147/152 - the 5 failures are all pre-existing/unrelated: a stale hardcoded account number in an old test, a renamed `news_client` attribute reference in an old test, and 2 deprecated `asyncio.get_event_loop()` WebSocket tests).

## Session 31c Update (2026-07) — Fixed QuickTradePanel Ambiguity, Order-Refresh Speed, History Limit, Manual-Sell Dedup Gap (Verified)
- **"% of buy pegged to portfolio not buying power" - resolved as NOT a bug, but a genuine display ambiguity**: user's account has EXACTLY 4x margin (buying_power ≡ 4×portfolio_value, always), so mathematically 25% of buying power ALWAYS equals exactly 100% of portfolio value in dollar terms - visually indistinguishable from the (already-fixed) bug. Fixed by making `QuickTradePanel.js`'s cost label show its math transparently: "~$X (Y% of BP)" in percent mode, with a full breakdown tooltip - removes all future ambiguity.
- **"Trades wait to show until next scan" fixed**: `App.js`'s `onOrderPlaced` callback only fetched account/positions/orders ONCE immediately after a trade - if Alpaca hadn't registered the fill yet at that exact instant, the user had to wait for the next full 30s base poll to find out if their trade went through. Now fires 3x (immediate + 1.5s + 4s).
- **"Some history seems to be missing" - real limitation found+fixed**: `History.js` only ever fetched the most recent 100 trades with zero pagination. Bumped to 2000.
- **HIGH-priority gap found by testing** (`iteration_32.json`): the Session 31b stop-loss dedup fix only covered the 3 AUTOMATIC exit paths in `position_monitor_service.py` - the MANUAL sell endpoint (`POST /api/orders`) had no equivalent check, so a manual Sell/Sell All click could still 500 with a raw Alpaca error if a stale order already held the shares. Fixed by routing manual sells through the same `position_monitor._sell_with_dedup()` helper (returns HTTP 409 with a clear message instead of a 500 if a sell is already pending).
- **Self-caught regression**: while wiring the above fix, an edit accidentally dropped `await db.orders.insert_one(order_doc)` + `return result` from `place_order()`, causing every manual SELL to return HTTP 200 with a `null` body (order actually succeeded server-side, just never confirmed to the caller) - caught via direct curl testing before it reached the testing agent, fixed same-turn.
- **Verified via `testing_agent_v4`**: `iteration_32.json` (3/3 new frontend items passed; also found the HIGH-priority manual-sell gap above) and `iteration_33.json` (both fixes re-verified live, including a live-reproduced concurrent-sell 409 race condition, not just mocked unit tests; full 45/45 pytest regression, one stale test constant fixed).

## Next Action Items (updated)

1. Investigate the pre-existing Trading.js multi-chart comparison view stuck on "Loading {symbol}..." indefinitely (P1, still open, carried over from Session 30).
2. Market Holiday awareness (P1) — auto-trader currently only skips weekends, will still attempt to trade on market holidays.
3. Refactor `server.py` and `auto_trader_service.py` (both now well over the 700-line guideline) into modular files (P2, tech debt).
4. Fix `alpaca_service.get_position_entry_time()`'s `OrderStatus.FILLED` enum bug (same class of bug already fixed for `get_orders()`/`get_open_orders()` this session, just not this specific method) - degrades gracefully via fallback timestamp today, not user-facing, but worth cleaning up for accurate trade-history hold-times (P2/P3, found `iteration_33.json`).
5. Scanner cache in `scanner_service.py` should be keyed by criteria hash, not global (P2, found Session 30).
## Session 31e Update (2026-07) — Fixed News Ranking (Sort + Sentiment Badges) (Verified)
- **"News is coming in now, but its not being ranked at all" - fixed**: the `score`/`sentiment` fields from Session 31d's fix already existed on every article, but (1) the list was never actually sorted by relevance (left in raw chronological API order) and (2) `NewsFeedPanel.js` never displayed any visual indicator of score/sentiment at all - so the ranking data existed but was invisible. Fixed: `check_alpaca_news()` (Benzinga/Alpaca) and `search_stock_news()` (Google News fallback) both now sort `articles` by `score` descending before returning. Also fixed `score_headline()`'s sentiment labeling - score==0 articles were mislabeled `'weak'` (implying some positive signal) - now correctly labeled `'neutral'`. Frontend: `NewsFeedPanel.js` now shows a colored badge per article (CATALYST/orange for strong_catalyst, MOMENTUM/blue, MENTION/gray for weak) - neutral (score 0) articles intentionally get no badge, staying visually quiet below the ranked ones.
- **Verified via `testing_agent_v4`** (`iteration_35.json`): 20/20 new pytest tests + live curl across 7 symbols confirmed non-increasing score order and correct neutral labeling; Playwright end-to-end confirmed real badges rendering with correct colors on live symbols (VEEE=MENTION, CJMB=CATALYST), unbadged neutral articles correctly below badged ones, zero console errors. Confirmed auto-trader's strict entry-decision path is completely unaffected (sorting is purely cosmetic for the display panel).
- Testing flagged one pre-existing, unrelated issue for visibility (not fixed this session, not caused by this change): React "duplicate key" console warnings on the Missed Opportunities tab (`MissedOpportunities.js` - likely needs `id+index` combined keys).

## Next Action Items (updated)

1. Fix React duplicate-key console warnings on the Missed Opportunities tab (`MissedOpportunities.js`) - cosmetic/console-only, found `iteration_35.json`, P3.
2. Investigate the pre-existing Trading.js multi-chart comparison view stuck on "Loading {symbol}..." indefinitely (P1, still open, carried over from Session 30).
3. Market Holiday awareness (P1) — auto-trader currently only skips weekends, will still attempt to trade on market holidays.
4. Refactor `server.py` and `auto_trader_service.py` (both now well over the 700-line guideline) into modular files (P2, tech debt).
5. Fix `alpaca_service.get_position_entry_time()`'s `OrderStatus.FILLED` enum bug (P2/P3, found `iteration_33.json`).
6. Scanner cache in `scanner_service.py` should be keyed by criteria hash, not global (P2, found Session 30).
7. Follow `/app/deploy/linux/README.md` / `/app/deploy/windows/README.md` to redeploy the current codebase to the user's production VPS - this now includes ALL fixes from Session 31 through 31e, none of which the VPS has yet.
8. Telegram/SMS alerts for new 5/5 scanner hits + trade executions — explicitly declined by user; leave as backlog only if they change their mind.

## Session 32 Update (2026-07) — Stop-Loss Reactive Retry, News Temperature (Hot/Medium/Cold), News Auto-Load, Multi-Chart Stuck-Loading Fix (Verified)

- **P0 CRITICAL FIX - missed exit / stuck position risk**: root-caused a real historical incident (ATPC, 2026-07-16 logs) where a stop-loss correctly triggered every ~2s for **1066 seconds (~17.8 min)** but never executed — `place_market_order` kept getting rejected by Alpaca with "insufficient qty available" because an earlier partial-take-profit sell order was still resting/unfilled and `_sell_with_dedup()`'s proactive `get_open_orders()` pre-check failed to detect that resting order the whole time (transient API issue), so the code just logged the rejection and waited on the next tick's same pre-check, forever. Price fell from $4.80 to $4.29 unprotected before the stale order finally got caught by luck. **Fix** (`position_monitor_service.py::_sell_with_dedup`): wrapped the final `place_market_order` call in a try/except that reacts directly to Alpaca's own "insufficient qty" rejection — force-cancels any resting sell order(s) for that symbol via `get_open_orders`+`cancel_order` and retries the sell ONCE, immediately, in the same call, instead of depending on the next tick's (unreliable) proactive check. Applies uniformly to all 3 exit paths that share this helper (stop-loss, partial take-profit, bearish-crossover exit) and the manual sell endpoint.
- **News "temperature" (Hot/Medium/Cold) was driven by article AGE, not catalyst strength - fixed**: user reported generic price-action headlines ("stock surges") getting flagged "Hot" same as real catalysts (merger/FDA/earnings) just for being recently published. Root cause: the flame-icon color (`NewsFlame`) was keyed off `freshness`/`classify_freshness()` (breaking/warm/cold = article age), completely separate from the existing `score_headline()` sentiment tiering (strong_catalyst/momentum/weak, already correctly penalizing "surge"/"rally" below real catalysts). Fixed: `score_headline()` now also returns a `temperature` field (hot/medium/cold/null) derived directly from sentiment tier, independent of age; `google_news_service.search_stock_news()` and `scanner_service.check_alpaca_news()`/`_check_candidate_news()` all propagate it (`news_temperature` on scan rows, `temperature` per article). Frontend `NewsFlame` (ScannerCells.js) now colors by `temperature` (age is still shown separately as plain "Xd ago" text, just no longer drives the flame color). Live-verified: a real "Apple Stock Hits All-Time Highs" headline (pure price-action, freshest/breaking) now correctly returns `temperature: "medium"`, not `"hot"`.
- **News auto-load for 3/5+ stocks (feature gap closed)**: `NewsFeedPanel.js` previously only fetched news when a symbol was manually clicked. Now accepts a new `scannerResults` prop (wired from `Dashboard.js`) - when no symbol is manually selected, it auto-fetches+merges news (score-sorted, symbol-tagged, capped at 6 symbols) for every scanner row with `criteria_count >= 3`, showing "News - N stocks @ 3/5+" in the header. Clicking a row/chip switches back to single-symbol mode as before.
- **Trading page multi-chart permanently stuck on "Loading {symbol}..." - root-caused+fixed**: `loadedChartsRef` (guards against re-fetching an already-open chart's full history) was marking a symbol "loaded" BEFORE its `fetchStockData()` call resolved - a single transient failure (timeout/429/etc on any of the 4 parallel bar/quote requests) permanently blocked all future retries for that symbol, leaving it stuck on "Loading..." forever until the chart was closed and reopened. Fixed: `fetchStockData()` now returns true/false and only marks `loadedChartsRef` complete on success; on failure it sets a new `chartLoadErrors[symbol]` state so the UI shows a "Failed to load chart data - Retry" button instead of an infinite spinner, and the symbol also auto-retries on the next periodic scanner/positions poll (15-60s) since it's not marked loaded. Added a separate `fetchingChartsRef` in-flight guard to prevent duplicate concurrent fetches while the first attempt is still pending.
- **Verified via `testing_agent_v4`** (`iteration_36.json`): 100% pass, backend+frontend, no regressions. 15/15 backend pytest (5 new tests in `test_stop_loss_dedup_fix.py` covering the reactive force-cancel-retry logic - correct "insufficient qty" substring match, only sell-side orders cancelled, unrelated exceptions still re-raise, no infinite loop on a second failure). Live manual BUY→SELL round trip (AAPL) succeeded cleanly, no 409/500. Live curl across AAPL/TSLA/NVDA/AMD confirmed `temperature` correctly derived from score/sentiment with zero inconsistencies. Live Playwright E2E (real login) confirmed the auto-load panel switching cleanly between "N stocks @ 3/5+" and single-symbol modes, and 3 simultaneously-selected Trading-page charts all loaded within ~4s with no stuck-loading testid. BUG FIX 4's actual error/retry-button path was code-reviewed only (no live fetch failures occurred during the test window since market data was available) - not a live-triggered gap, just noted for a future agent if they want to force it.

## Next Action Items (updated)

1. Redeploy the current codebase (Session 32 fixes included) to the user's production VPS - see `/app/deploy/linux/README.md` / `/app/deploy/windows/README.md`.
2. Market Holiday awareness (P1) — auto-trader currently only skips weekends, will still attempt to trade on market holidays.
3. Refactor `server.py` and `auto_trader_service.py` (both well over the 700-line guideline) into modular files (P2, tech debt).
4. Fix React duplicate-key console warnings on the Missed Opportunities tab (`MissedOpportunities.js`) - cosmetic/console-only, P3.
5. Fix `alpaca_service.get_position_entry_time()`'s `OrderStatus.FILLED`/`order_class` enum deprecation warning (P3).
6. Scanner cache in `scanner_service.py` should be keyed by criteria hash, not global (P2).
7. Telegram/SMS alerts for new 5/5 scanner hits + trade executions — explicitly declined by user; leave as backlog only if they change their mind.

## Session 33 Update (2026-07) — "Scalping Trade (No News)" Mode: Auto-Trader Entry Path for No-Catalyst Momentum Gappers (Verified)

- **New feature (user-provided detailed spec)**: stocks that gap massively with ZERO underlying news catalyst (pure retail-momentum/short-squeeze, high T12-halt risk) were previously either fully ignored (auto-trader's 5/5 gate strictly requires a real news catalyst) or traded with the same risk parameters as a real-catalyst setup if bought manually. Added an explicit, OPT-IN (off by default), reduced-risk "Scalping Trade (No News)" mode per the user's choices:
  - **Scanner** (`scanner_service.py`): new `no_news_scalp_candidate` field on scan rows - True only when criteria_count==4 with `positive_news=False` and the other 4 pillars (price_range/pct_change/volume_ratio/float) all verified-true. Computed in the same authoritative post-verification recompute pass as `criteria_count`/`ready_to_trade`.
  - **Auto-trader** (`auto_trader_service.py`): new `check_no_news_scalp_entry()` entry path - requires the SAME First-Pullback technical pattern + volume/MACD/SMA confirmation as normal entries (news is the ONLY pillar excused, technicals are never skipped), computes the structural (pullback-low) stop the same way, but rejects (skips, never clamps) if the resulting risk% exceeds a TIERED max-stop cap (`_no_news_max_stop_pct`: 5% for $2-5 stocks, 3% for $5-10, 1.5% for $10-20 - a flat % is wrong here since 5% of a $20 stock is a $1 cushion that breaks the pattern long before triggering). `execute_entry()` hard-caps position size to `no_news_position_size_pct` (25% of the normally-calculated size) and tags the position `is_no_news_scalp=True`/`strategy='Scalping Trade (No News)'`. `monitor_exits()` applies a shorter `no_news_bailout_seconds` (60s vs normal 90s) time-stop only to these tagged positions. `process_scanner_results()` checks these candidates only AFTER all normal 5/5 real-catalyst signals have had first claim on position slots, and only when `no_news_scalp_enabled` (new setting, defaults False) is explicitly turned on.
  - **Frontend**: new "NO CATALYST" T12-halt-risk red warning badge (`T12HaltBadge` in ScannerCells.js) rendered on ANY scanner table row hitting `no_news_scalp_candidate` (always visible, not just at order time). New "⚠ Scalping Trade (No News)" opt-in toggle in the Trading page's "Entry Conditions" panel (defaults unchecked), wired to `POST /api/auto-trader/settings {no_news_scalp_enabled}`, persists across refresh via the existing settings-fetch-on-mount pattern.
  - New `/app/backend/tests/test_no_news_scalp_mode.py` (17 tests: candidate-flag logic, tiered stop-cap lookup, entry gating/opt-in, position-size reduction + strategy tagging, per-position bailout selection).
- **Verified via `testing_agent_v4`** (`iteration_37.json`): 100% pass, no critical/UI bugs. 17/17 new pytest + full regression suite (193 passed, 2 pre-existing unrelated websocket test failures). Live backend confirmed `no_news_scalp_enabled` defaults False, settings round-trip works and was reset to False after testing. **Live market data produced 2 genuine `no_news_scalp_candidate` rows (VIVK, GREE)** during the test window, proving the logic is correct on real data, not just mocked - the T12 "NO CATALYST" badge rendered live on both with the correct tooltip. Trading page toggle confirmed defaulting unchecked, toggling on/off with persistence across a real page reload, left OFF (safe default) at the end. One low-priority gap found (demo_scanner_service.py's `/api/scanner/demo` endpoint didn't set the field at all, since demo mode always simulates `positive_news=True` by construction) - fixed same-session by adding the field (always False there, by design - demo mode doesn't simulate a no-catalyst scenario).

## Session 34 Update (2026-07) — News: Raw/Unfiltered Refactor + All-Stocks Cadence Pre-fetch, Instant-on-Click Display (Verified)

- **"We shouldn't be filtering the news we receive - raw news should come in, then it's sorted into levels" - refactored**: `google_news_service.py::score_headline()` now NEVER drops a headline; `min_score` in `search_stock_news()`/`scanner_service.check_alpaca_news()` is used ONLY to compute the derived `has_news`/`has_real_catalyst` boolean (the auto-trader's strict entry-decision signal) - the returned `articles` list is always the full raw set, sorted by score, tagged with sentiment/temperature for the caller/UI to render however it needs. Added `DEAL_INDICATOR_WORDS` + a `$10M+`-in-a-deal-headline regex bonus (bumps score to 10/strong_catalyst) after finding VIVK's real $289M crude-oil-marketing catalyst was mis-scored WEAK (score 2) by dry corporate wording ("Expands...Announces...Transactions Worth $289M") missing the obvious keyword tiers.
- **Classification logic** (`score_headline()`, unchanged structure, documented for user's "what logic are we using to classify?" question): 3 positive keyword tiers - STRONG_CATALYSTS (score 10: FDA/M&A/earnings-beat/upgrade/contract-win/partnership/product-launch/IPO), MOMENTUM_KEYWORDS (score 5: pure price-action words like surge/rally/breakout with no explained reason), WEAK_POSITIVE_KEYWORDS (score 2: generic gains/up/announces/expands). NEGATIVE_KEYWORDS (plunge/crash/lawsuit/downgrade/bankruptcy/etc.) always override to `sentiment: negative` regardless of matched score. `temperature` (flame icon color) is derived purely from this tier - hot=strong_catalyst, medium=momentum, cold=weak, negative=negative, no icon=neutral (score 0) - explicitly NOT article age (age is tracked separately as plain "Xd ago" text via `classify_freshness()`).
- **"News should be pulled for all stocks at a cadence, shouldn't need to load once I click" - implemented**: previously only the scanner's top-50 (by estimated criteria_count) candidates got a news check at all, and only a single headline (`limit=1`) for the `has_news` boolean - the frontend `NewsFeedPanel.js` then fired a FRESH `axios.get(/api/news/{symbol})` on every row click/every auto-refresh, causing a visible loading delay. Fixed: new `scanner_service.py::_attach_full_news_articles()` pre-fetches the FULL raw article list (`limit=8`) for **every** row in the scan results (not just top-50), running concurrently (4th job in the existing outer thread pool alongside volume/float/top-50-news) so it adds no extra wall-clock time to the ~60s scan cadence. `check_single_news()` (top-50 pass) also bumped to `limit=8` and stores the same `news_articles` field, so top-50 rows aren't double-fetched. Every scanner API response row now always has `news_articles` as an array (`[]` if none found), never `null`. `NewsFeedPanel.js` rewritten to read `row.news_articles` directly from the already-fetched `scannerResults` prop for BOTH manual (clicked symbol) and auto (3/5+ merged) modes - zero network call, zero loading state, in the common case. A live `axios.get` fallback remains only for a symbol not currently present in scan results (e.g. an open position that dropped below scan criteria).
- **Verified via `testing_agent_v4`** (`iteration_39.json`): 100% pass. Backend curl: `/api/scanner/scan` returned 28 rows, 0 with `null` `news_articles` (16 non-empty, 12 empty-array) - confirms the pre-fetch always completes before the response returns. Frontend Playwright: 5 different scanner-row clicks (ADVB/LABT/INM/OTLY/re-click ADVB) all updated the News Feed Panel **instantly** with zero visible loading state; network monitoring confirmed **zero** `/api/news/{symbol}` XHR calls were triggered by any of the clicks (all used the cached-instant path). Auto-merged "N stocks @ 3/5+" mode also confirmed instant/no-fetch. Existing top-50 single-flame scanner-table badges confirmed unaffected (pure regression check, that logic wasn't touched). Zero console errors, including with neutral/no-badge articles rendering.

## Session 35 Update (2026-07) — Real Bracket Order at Time of Buying (Root-Cause Fix + Verified) + Daily-Loss Kill-Switch Blind Spot Found

- **User-reported bug investigated**: two real trades today (SNTG -29.57%/$951, OTLY -10.32%/$126) stopped out for far more than the configured 1% stop-loss. RCA: both filled during abnormally wide bid/ask spreads (28%, 9.5%) via the manual Quick-Trade buy-with-stop endpoint (`stop_type='trailing'`, the QuickTradePanel default). That path placed ONLY a plain market buy with ZERO real stop-loss/take-profit order resting on the exchange - the software `position_monitor_service` poll loop (with a 60s "settling" delay) was the only protection, and its stop was calculated from the (already-crashed) bid while entry was recorded at the ask, baking in most of the loss the instant the trade filled.
- **User's explicit direction** (declined a spread-rejection guard): *"No there should have been a stop loss put in place at the time of buying same with take profit."* Implemented exactly that:
  - `server.py` POST /api/orders (buy path) and `auto_trader_service.py::execute_entry()` now **always** attempt a real Alpaca **bracket order** (resting stop-loss + take-profit legs) immediately at entry, regardless of `stop_type` - falling back to a plain market order only if the bracket placement itself raises. The software monitor (`position_monitor_service`/`auto_trader.monitor_exits()`) still layers dynamic trailing-up/partial-sell/breakeven management on top; the bracket is the immediate hard floor.
  - `auto_trader_service.sell_with_retry()` gained the same "force-cancel resting order + retry on insufficient qty" safety net already proven in `position_monitor_service._sell_with_dedup`, so bracket-leg coexistence doesn't block the bot's own exits.
  - New `/app/backend/tests/test_bracket_order_at_entry.py` (5 tests): bracket-first + fallback for `execute_entry`, force-cancel-retry + unrelated-exception passthrough for `sell_with_retry`, live curl smoke test confirming `order_class:'bracket'` on a real buy.
- **Regression found + fixed same session** (via `testing_agent_v4`, `iteration_40.json`): selling within ~10s of a bracket buy failed (409 "sell already pending" 1-9s after, raw 500 "cannot open a short sell while a long buy order is open" at 0s) - `position_monitor_service._sell_with_dedup`'s old "wait up to 10s for an existing sell order to fill" logic (written before brackets-at-entry existed, to dedupe genuinely-duplicate in-flight sells) misidentified every bracket's own resting stop-loss leg as a blocking duplicate. Fixed: `_sell_with_dedup` now **force-cancels any resting sell order immediately** (no staleness wait - the bracket leg or a stale duplicate, either way a deliberate new sell supersedes it) and **briefly polls (up to 2s) for an unfilled parent BUY order** before selling, raising a clean message (surfaced as HTTP 409, not a raw 500) if it still hasn't filled. `server.py`'s sell handler now catches that specific message and returns 409. Updated `test_stop_loss_dedup_fix.py`'s tests to match the new "always force-cancel" contract (old "skip if <10s old" test replaced; buy-order test now verifies poll-then-proceed and poll-then-clean-error).
- **Live-verified** (main agent, post-fix): both regression scenarios reproduced then confirmed fixed via curl against the running backend - buy→sell with a 0s gap and a 1s gap both now return HTTP 200 and the position closes flat correctly. Full pytest suite: `test_stop_loss_dedup_fix.py` + `test_bracket_order_at_entry.py` + `test_no_news_scalp_mode.py` = 38/38 passed. Settings page copy ("All stops are software-managed") updated to reflect the new bracket-at-entry behavior.
- **New finding surfaced to user, NOT yet acted on** - **the "1% daily max loss" hard kill-switch (`auto_trader.daily_pnl`/`check_risk_limits`) only tracks P&L from trades opened+closed through the autonomous bot's own `execute_entry`/`monitor_exits()` loop** (`self.daily_pnl += pnl` only fires there). Manually-placed trades via the Quick Trade Panel (confirmed to be the origin of SNTG/OTLY) are tracked entirely separately via `position_monitor_service` and never touch `auto_trader.daily_pnl` - so the daily-loss kill switch would NOT have halted trading even at a 54% real portfolio drawdown, because it was never designed to see manually-placed losses at all. Needs the user's direction on whether the kill-switch should account for ALL trading (manual + bot) and whether it should also block/warn on further MANUAL buys once tripped, not just pause the bot.
- Also explicitly SKIPPED per user request: the `strategy: 'Auto-trader'` mislabeling in `trade_history` (hardcoded regardless of manual vs bot origin) - left as-is for now, assuming defaults.

## Session 35b (same day) — "Should Have Hit Profit But Didn't Sell" - Stale Pre-Trade Quote Used as Tracked Entry Price (Root-Cause Fix + Live Correction)

- **User report** (with chart screenshot showing ENTRY $15.59 / TARGET $15.91 / current price already well above target, but no sell happened): found the bracket-order-at-entry fix (Session 35a) computes `entry_price` for `position_monitor` tracking from the **pre-trade quote** (the ask fetched right before submitting the order), not the market order's **real fill**. On a normal spread these are nearly identical, but on the same abnormally-wide-spread stocks this whole investigation has been about, a market order's actual fill can land far from that pre-trade quote - real incident: ADVB quoted Ask $17.73/27.5% spread, actual bracket fill was $15.59 (a 12% gap). Every downstream %-based trigger (`take_profit_pct`, `trailing_stop_pct`, `move_to_breakeven`) is computed off `entry_price`, so tracking anchored to a stale, too-high quote meant the +2% partial-take-profit target needed a much bigger real move than intended - price legitimately blew past the true +2% level (visible on the frontend chart, which correctly draws its reference lines off Alpaca's real `avg_entry_price`) while the backend's own trigger, anchored to $17.73, never fired.
- **Fix** (`server.py`, bracket-order buy path): after a successful bracket order placement, polls `alpaca_service.get_order()` for the parent buy leg's real `filled_avg_price` (same pattern already used elsewhere for the plain-market-order branch) and uses that for `position_monitor.add_position()`'s `entry_price`, instead of the pre-trade quote. The bracket's actual resting stop/target leg PRICES (submitted to Alpaca before the fill, computed from the bid-at-entry) are unaffected/unchanged - only the software layer's tracked reference point is corrected.
- **Live-corrected the currently-open ADVB position** (309 shares, real cost basis $15.59356 confirmed via Alpaca) that was still tracked with the stale `entry_price: 17.73`/`highest_price: 17.73` from before this fix - directly patched the persisted `monitored_positions` MongoDB document (stopped backend first to avoid a save-state race that reverted the first attempt, then restarted) so live tracking is accurate going forward. Also cleaned up a stray `TEST` entry left in `auto_trader_state.open_positions` from this session's own testing (was consuming a real trading slot).
- Full pytest suite (`test_stop_loss_dedup_fix.py` + `test_bracket_order_at_entry.py`, 21 tests) re-verified passing after this change.

## Session 35d (same day) — Production VPS Login Troubleshooting (Resolved)

- User locked out of the production VPS login after changing `ADMIN_PASSWORD` in `backend/.env`. RCA walked through step by step: (1) `sudo supervisorctl restart backend` did nothing - VPS uses **systemd** (`momentumx-backend.service`/`/app/deploy/linux/`), not supervisor (supervisor only exists in this sandbox); correct command is `sudo systemctl restart momentumx-backend`. (2) Added logging to `auth.py::seed_user()` (previously silent) so `journalctl -u momentumx-backend` shows exactly whether it created/updated/left-unchanged the password hash on each restart - confirmed `DanM(828689)` was already correctly hashed and matching in MongoDB. (3) Direct `curl .../api/auth/login` from the VPS itself confirmed the backend/password/database were all correct (`access_token` returned) - proved the issue was NOT the password at all. (4) Browser Network tab showed `net::ERR_CONNECTION_REFUSED` on the login XHR - a pure network-reachability issue, not an auth issue. **Real root cause**: the SSH tunnel used to access the VPS's webapp was missing the backend port forward (`-L 9001:127.0.0.1:9001`) - only the frontend port (4000) was tunneled, so the browser's API calls to `127.0.0.1:9001` hit the user's own laptop (nothing listening) instead of the VPS. Reconnecting with both `-L` flags (per `/app/deploy/linux/README.md`) resolved it completely.
- Also fixed along the way: `git pull` on the VPS failed with "dubious ownership" (git safety check since the repo dir is owned by the `momentumx` service user while operating as root) - resolved via git's own suggested `git config --global --add safe.directory /opt/autotrader`.
- Takeaway for future deployment troubleshooting: always confirm systemd vs supervisor first (this sandbox's restart commands don't apply 1:1 to the user's VPS), and remember `REACT_APP_BACKEND_URL`/other `REACT_APP_*` values are baked into the frontend build at `yarn build` time, not read at runtime - a frontend/.env edit alone does nothing without a rebuild.

## Session 35c (same day) — "Missed a 2000% Move" Investigation (ZYBT, CPHI, LABT) + Max-Float Filter Raised to 45M

- **Investigated 3 user-reported missed massive movers**: ZYBT (07-20, 0.75→8.01, +530-1047% multi-day), CPHI (07-21, open $0.86→high $19.19, **+2131%**), LABT (07-22, prior close $1.86→open $6.49, +249%).
- **ZYBT & LABT**: confirmed via `/api/missed-opportunities` - both hit `criteria_count: 4` with price/pct_change/volume_ratio/float ALL passing (`criteria_met`), the only failing pillar `positive_news: false` (pure momentum/short-squeeze, no real news catalyst found by either Alpaca/Benzinga or Google News). Correctly logged as "Missed Opportunity - Missing: No positive news" rather than traded blind.
- **CPHI**: confirmed via SEC EDGAR (`alpaca_service.get_float_data('CPHI')`) - 40,522,002 shares outstanding, over 2x the (then) 20M max-float filter - excluded by the float criteria regardless of its price action, since the scanner is deliberately built to target only low-float stocks (Ross Cameron's core edge: fewer shares means a given amount of buying pressure moves price further).
- **User correction**: initially told the user `no_news_scalp_enabled` defaults OFF and was off for all 3 - user stated it was ON at the time (for at least one of these). Backend settings-change logs (`grep "no_news_scalp_enabled"`) only show toggle events on 07-21 21:37-21:43 (a narrow 6-min window, consistent with the PREVIOUS session's `testing_agent_v4` feature-verification run, not sustained live trading use) - **not yet fully reconciled with the user's correction**; deprioritized per user's explicit direction to move straight to the float filter change instead. **Flagged as an open item** - if the no-news-scalp toggle genuinely was on and still didn't fire for one of these symbols, there may be a separate real bug in `check_no_news_scalp_entry()`'s gating (technical pattern match, tiered-stop-cap, or position-slot priority) worth revisiting.
- **Fix implemented**: raised `max_float` default from 20,000,000 to **45,000,000** everywhere it's hardcoded (comfortable margin above CPHI's 40.5M) - `server.py` (Pydantic field default, `/auto-trader/process` manual-trigger criteria, `/scanner/demo` default, and critically the **live `auto_trader_loop()`'s own scan criteria** used by the actual autonomous bot), `scanner_service.py` (both the `scan_market()` default-criteria dict and `_calculate_accurate_float()`'s fallback default), and the frontend (`useGlobalScanner.js`'s shared default criteria, `Trading.js`'s scanner-fetch criteria). Verified: CPHI's real 40.5M float now numerically passes (`shares_outstanding <= max_float`). Full pytest suite (`test_dual_key_and_sizing.py`, `test_float_and_analytics.py`) still 11 passed/1 skipped - those tests pass their own explicit `max_float` in the request payload so are unaffected by the default-value change.

## Next Action Items (updated)

1. **User decision needed**: should the daily-loss kill-switch (currently bot-trades-only) also track manual Quick-Trade-Panel P&L, and should tripping it also restrict/warn on manual buys (not just pause the autonomous bot)? (P0/P1 candidate, pending user input)
2. A pre-existing open position 'ADVB' (opened before this session's fix, 27.6% spread at entry) currently has no real resting bracket order and a software stop set far looser than intended (computed from the crashed bid-at-entry) - flagged to user for a decision on whether to manually tighten/replace its stop now.
3. Redeploy the current codebase (Sessions 32-35 fixes/features included) to the user's production VPS - see `/app/deploy/linux/README.md` / `/app/deploy/windows/README.md`.
4. Market Holiday awareness (P1) — auto-trader currently only skips weekends, will still attempt to trade on market holidays.
5. Refactor `server.py` and `auto_trader_service.py` (both well over the 700-line guideline) into modular files (P2, tech debt).
6. Fix React duplicate-key console warnings on the Missed Opportunities tab (`MissedOpportunities.js`) - cosmetic/console-only, P3.
7. Fix `alpaca_service.get_position_entry_time()`'s `OrderStatus.FILLED`/`order_class` enum deprecation warning (P3).
8. Scanner cache in `scanner_service.py` should be keyed by criteria hash, not global (P2).
9. Telegram/SMS alerts for new 5/5 scanner hits + trade executions — explicitly declined by user; leave as backlog only if they change their mind.
10. Consider surfacing `no_news_stop_tiers`/`no_news_position_size_pct`/`no_news_bailout_seconds` as editable UI inputs (currently backend-configurable via API only, sensible hardcoded defaults per user's spec) if the user wants to tune them without an API call.
11. `trade_history`'s `strategy: 'Auto-trader'` mislabeling (manual vs bot origin) - explicitly skipped this session, revisit if user wants accurate trade attribution later.
